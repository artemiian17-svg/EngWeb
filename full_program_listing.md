# Full Program Listing

## index.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РџСЂР°РєС‚РёРєР° РёРЅРѕСЃС‚СЂР°РЅРЅС‹С… СЏР·С‹РєРѕРІ</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="home">
  <div id="site-header"></div>

  <main class="page">
    <section class="hero-banner">
      <div class="hero-copy">
        <p class="eyebrow">РџР РђРљРўРРљРђ РРќРћРЎРўР РђРќРќР«РҐ РЇР—Р«РљРћР’</p>
        <h1>РџРµСЂРµС…РѕРґРёС‚Рµ РѕС‚ РїСЂР°РІРёР» Рє Р¶РёРІРѕРјСѓ РѕР±С‰РµРЅРёСЋ</h1>
        <p class="lead">
          LingoFlux РїРѕРјРѕРіР°РµС‚ СЂРµРіСѓР»СЏСЂРЅРѕ РїСЂР°РєС‚РёРєРѕРІР°С‚СЊ Р°РЅРіР»РёР№СЃРєРёР№, РёСЃРїР°РЅСЃРєРёР№, РЅРµРјРµС†РєРёР№ Рё С„СЂР°РЅС†СѓР·СЃРєРёР№
          С‡РµСЂРµР· РјРёРЅРё-СѓСЂРѕРєРё, СЂР°Р·РіРѕРІРѕСЂРЅС‹Рµ СЃС†РµРЅР°СЂРёРё Рё СѓРїСЂР°Р¶РЅРµРЅРёСЏ РЅР° Р·Р°РєСЂРµРїР»РµРЅРёРµ СЃР»РѕРІ.
        </p>
        <div class="action-row">
          <a class="button solid" href="services.html">РЎРњРћРўР Р•РўР¬ РЈРЎР›РЈР“Р</a>
          <a class="button ghost" href="lessons.html">РћРўРљР Р«РўР¬ РЈР РћРљР</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="hero-card">
          <span>РЎР•Р“РћР”РќРЇРЁРќРЇРЇ РџР РђРљРўРРљРђ</span>
          <strong>TRAVEL SPEAKING</strong>
          <p>8 СЃР»РѕРІ, 1 РґРёР°Р»РѕРі, 1 РєРѕСЂРѕС‚РєРѕРµ СѓРїСЂР°Р¶РЅРµРЅРёРµ РЅР° РїРµСЂРµРІРѕРґ Рё РїСЂРѕРёР·РЅРѕС€РµРЅРёРµ.</p>
        </div>
        <div class="hero-card accent">
          <span>РђРљРўРР’РќРћРЎРўР¬</span>
          <strong>16 Р”РќР•Р™ РџРћР”Р РЇР”</strong>
          <p>РќРµР±РѕР»СЊС€РѕР№, РЅРѕ РїРѕСЃС‚РѕСЏРЅРЅС‹Р№ С‚РµРјРї РїРѕРјРѕРіР°РµС‚ Р±С‹СЃС‚СЂРµРµ Р·Р°РєСЂРµРїР»СЏС‚СЊ СЏР·С‹Рє.</p>
        </div>
      </div>
    </section>

    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">РџР Р•РРњРЈР©Р•РЎРўР’Рђ</p>
        <h2>РЎРёР»СЊРЅС‹Рµ СЃС‚РѕСЂРѕРЅС‹ Рё РєР»СЋС‡РµРІС‹Рµ С†РµРЅРЅРѕСЃС‚Рё СЃРµСЂРІРёСЃР°</h2>
      </div>
      <div class="card-grid">
        <article class="feature-card">
          <h3>РџСЂР°РєС‚РёС‡РµСЃРєРёР№ РїРѕРґС…РѕРґ</h3>
          <p>РљР°Р¶РґС‹Р№ РјРѕРґСѓР»СЊ СЃС‚СЂРѕРёС‚СЃСЏ РІРѕРєСЂСѓРі Р¶РёР·РЅРµРЅРЅС‹С… СЃРёС‚СѓР°С†РёР№: РїСѓС‚РµС€РµСЃС‚РІРёСЏ, СЂР°Р±РѕС‚Р°, РІСЃС‚СЂРµС‡Рё Рё РїРѕРІСЃРµРґРЅРµРІРЅРѕРµ РѕР±С‰РµРЅРёРµ.</p>
        </article>
        <article class="feature-card">
          <h3>РљРѕСЂРѕС‚РєРёРµ Р·Р°РЅСЏС‚РёСЏ</h3>
          <p>РЈСЂРѕРєРё СЂР°СЃСЃС‡РёС‚Р°РЅС‹ РЅР° 10вЂ“15 РјРёРЅСѓС‚ Рё Р»РµРіРєРѕ РІСЃС‚СЂР°РёРІР°СЋС‚СЃСЏ РІ РµР¶РµРґРЅРµРІРЅС‹Р№ РіСЂР°С„РёРє Р±РµР· РїРµСЂРµРіСЂСѓР·РєРё.</p>
        </article>
        <article class="feature-card">
          <h3>РќР°РіР»СЏРґРЅС‹Р№ РїСЂРѕРіСЂРµСЃСЃ</h3>
          <p>РџРѕР»СЊР·РѕРІР°С‚РµР»СЊ СЃ РїРµСЂРІС‹С… РјРёРЅСѓС‚ РІРёРґРёС‚ СЃС‚СЂСѓРєС‚СѓСЂСѓ РѕР±СѓС‡РµРЅРёСЏ Рё РїРѕР»СѓС‡Р°РµС‚ Р±С‹СЃС‚СЂС‹Р№ РѕРїС‹С‚ РІР·Р°РёРјРѕРґРµР№СЃС‚РІРёСЏ СЃ РєРѕРЅС‚РµРЅС‚РѕРј.</p>
        </article>
      </div>
    </section>

    <section class="section-block tinted">
      <div class="section-heading">
        <p class="eyebrow">РќРђРџР РђР’Р›Р•РќРРЇ</p>
        <h2>РЇР·С‹РєРё Рё РЅР°РІС‹РєРё, РєРѕС‚РѕСЂС‹Рµ СЂР°Р·РІРёРІР°РµС‚ РїР»Р°С‚С„РѕСЂРјР°</h2>
      </div>
      <div class="language-wrap">
        <div class="pill-row">
          <button class="language-pill active" data-language="РђРЅРіР»РёР№СЃРєРёР№">РђРќР“Р›РР™РЎРљРР™</button>
          <button class="language-pill" data-language="РСЃРїР°РЅСЃРєРёР№">РРЎРџРђРќРЎРљРР™</button>
          <button class="language-pill" data-language="РќРµРјРµС†РєРёР№">РќР•РњР•Р¦РљРР™</button>
          <button class="language-pill" data-language="Р¤СЂР°РЅС†СѓР·СЃРєРёР№">Р¤Р РђРќР¦РЈР—РЎРљРР™</button>
        </div>
        <div class="language-panel">
          <p>Р’С‹Р±СЂР°РЅРЅРѕРµ РЅР°РїСЂР°РІР»РµРЅРёРµ</p>
          <strong id="language-output">РђРЅРіР»РёР№СЃРєРёР№</strong>
          <span>Р›РµРєСЃРёРєР°, РіСЂР°РјРјР°С‚РёРєР°, Р°СѓРґРёСЂРѕРІР°РЅРёРµ Рё СЂР°Р·РіРѕРІРѕСЂРЅР°СЏ РїСЂР°РєС‚РёРєР° РїРѕ СѓСЂРѕРІРЅСЏРј РїРѕРґРіРѕС‚РѕРІРєРё.</span>
        </div>
      </div>
    </section>

    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">Р¤РћР РњРђРўР«</p>
        <h2>Р§С‚Рѕ РјРѕР¶РЅРѕ РґРµР»Р°С‚СЊ РЅР° РїР»Р°С‚С„РѕСЂРјРµ СѓР¶Рµ СЃРµР№С‡Р°СЃ</h2>
      </div>
      <div class="card-grid">
        <article class="feature-card">
          <h3>РџСЂРѕС…РѕРґРёС‚СЊ СѓСЂРѕРєРё</h3>
          <p>РџРѕРґР±РѕСЂРєР° РєРѕСЂРѕС‚РєРёС… РјРѕРґСѓР»РµР№ РїРѕ СѓСЂРѕРІРЅСЏРј Рё Р¶РёР·РЅРµРЅРЅС‹Рј СЃС†РµРЅР°СЂРёСЏРј СЃ РєРѕРЅРєСЂРµС‚РЅРѕР№ СѓС‡РµР±РЅРѕР№ С†РµР»СЊСЋ.</p>
        </article>
        <article class="feature-card">
          <h3>Р’С‹Р±РёСЂР°С‚СЊ СѓСЃР»СѓРіРё</h3>
          <p>Р Р°Р·РіРѕРІРѕСЂРЅР°СЏ РїСЂР°РєС‚РёРєР°, Р»РµРєСЃРёС‡РµСЃРєРёРµ С‚СЂРµРЅРёСЂРѕРІРєРё, Р°СѓРґРёСЂРѕРІР°РЅРёРµ Рё РїРµСЂСЃРѕРЅР°Р»СЊРЅР°СЏ С‚СЂР°РµРєС‚РѕСЂРёСЏ РѕР±СѓС‡РµРЅРёСЏ.</p>
        </article>
        <article class="feature-card">
          <h3>Р§РёС‚Р°С‚СЊ Р±Р»РѕРі</h3>
          <p>РњР°С‚РµСЂРёР°Р»С‹ Рѕ РґРёСЃС†РёРїР»РёРЅРµ РѕР±СѓС‡РµРЅРёСЏ, СЌС„С„РµРєС‚РёРІРЅРѕР№ РїСЂР°РєС‚РёРєРµ Рё РїРѕРґРіРѕС‚РѕРІРєРµ Рє СЂРµР°Р»СЊРЅС‹Рј СЃРёС‚СѓР°С†РёСЏРј РѕР±С‰РµРЅРёСЏ.</p>
        </article>
      </div>
    </section>

    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">Р‘Р«РЎРўР Р«Р™ РЎРўРђР Рў</p>
        <h2>РџРѕРїСЂРѕР±СѓР№С‚Рµ РїР»Р°С‚С„РѕСЂРјСѓ Р±РµСЃРїР»Р°С‚РЅРѕ</h2>
      </div>
      <div class="cta-panel">
        <div>
          <p class="lead compact">РџРѕР»СѓС‡РёС‚Рµ РїСЂРѕР±РЅС‹Р№ РґРѕСЃС‚СѓРї Рє РѕСЃРЅРѕРІРЅС‹Рј СЃС†РµРЅР°СЂРёСЏРј РїСЂР°РєС‚РёРєРё Рё РЅР°С‡РЅРёС‚Рµ Р·Р°РЅРёРјР°С‚СЊСЃСЏ СѓР¶Рµ СЃРµРіРѕРґРЅСЏ.</p>
        </div>
        <a class="button solid" href="contacts.html">РћРЎРўРђР’РРўР¬ Р—РђРЇР’РљРЈ</a>
      </div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="home.js"></script>
</body>
</html>
```

## services.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РЈСЃР»СѓРіРё</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="services">
  <div id="site-header"></div>

  <main class="page">
    <section class="page-intro">
      <p class="eyebrow">РЈРЎР›РЈР“Р</p>
      <h1>РџСЂРѕРіСЂР°РјРјС‹ РїСЂР°РєС‚РёРєРё РёРЅРѕСЃС‚СЂР°РЅРЅС‹С… СЏР·С‹РєРѕРІ</h1>
      <p class="lead"><strong>LingoFlux РїСЂРµРґР»Р°РіР°РµС‚ РїСЂР°РєС‚РёС‡РµСЃРєРёРµ С„РѕСЂРјР°С‚С‹ Р·Р°РЅСЏС‚РёР№, РѕСЂРёРµРЅС‚РёСЂРѕРІР°РЅРЅС‹Рµ РЅР° СЂРµР°Р»СЊРЅРѕРµ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёРµ СЏР·С‹РєР° Рё СѓСЃС‚РѕР№С‡РёРІС‹Р№ РїСЂРѕРіСЂРµСЃСЃ.</strong></p>
    </section>

    <section class="section-block">
      <div class="service-grid">
        <article class="service-item detailed">
          <span class="meta-text">Р¤РћР РњРђРў 01</span>
          <h3>Р Р°Р·РіРѕРІРѕСЂРЅС‹Рµ РјРёРЅРё-СЃРµСЃСЃРёРё</h3>
          <p>РљСЂР°С‚РєРёРµ СЃС†РµРЅР°СЂРёРё РѕР±С‰РµРЅРёСЏ РїРѕ С‚РµРјР°Рј РїСѓС‚РµС€РµСЃС‚РІРёР№, СЂР°Р±РѕС‚С‹, Р·РЅР°РєРѕРјСЃС‚РІ Рё РїРѕРІСЃРµРґРЅРµРІРЅРѕР№ Р¶РёР·РЅРё.</p>
          <ul class="service-points">
            <li>РЈСЂРѕРІРЅРё: A1вЂ“B2</li>
            <li>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ: 15 РјРёРЅСѓС‚</li>
            <li>Р¤РѕРєСѓСЃ: СЂРµС‡СЊ Рё СЂРµР°РєС†РёСЏ РІ РґРёР°Р»РѕРіРµ</li>
          </ul>
          <a class="button ghost" href="lessons.html">РџР•Р Р•Р™РўР Рљ РЈР РћРљРђРњ</a>
        </article>
        <article class="service-item detailed">
          <span class="meta-text">Р¤РћР РњРђРў 02</span>
          <h3>РЎР»РѕРІР°СЂРЅС‹Рµ С‚СЂРµРЅРёСЂРѕРІРєРё</h3>
          <p>Р—Р°РєСЂРµРїР»РµРЅРёРµ РЅРѕРІРѕР№ Р»РµРєСЃРёРєРё С‡РµСЂРµР· РїРѕРІС‚РѕСЂРµРЅРёРµ, РїРµСЂРµРІРѕРґ, Р°СЃСЃРѕС†РёР°С†РёРё Рё РєРѕСЂРѕС‚РєРёРµ Р·Р°РґР°РЅРёСЏ РЅР° СѓРїРѕС‚СЂРµР±Р»РµРЅРёРµ.</p>
          <ul class="service-points">
            <li>РўРµРјС‹: travel, work, daily life</li>
            <li>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ: 10 РјРёРЅСѓС‚</li>
            <li>Р¤РѕРєСѓСЃ: Р°РєС‚РёРІРЅС‹Р№ СЃР»РѕРІР°СЂСЊ</li>
          </ul>
          <a class="button ghost" href="lessons.html">РќРђР§РђРўР¬ РўР Р•РќРР РћР’РљРЈ</a>
        </article>
        <article class="service-item detailed">
          <span class="meta-text">Р¤РћР РњРђРў 03</span>
          <h3>РџСЂР°РєС‚РёРєР° Р°СѓРґРёСЂРѕРІР°РЅРёСЏ</h3>
          <p>РџСЂРѕСЃР»СѓС€РёРІР°РЅРёРµ СЂРµРїР»РёРє СЂР°Р·Р»РёС‡РЅРѕР№ СЃР»РѕР¶РЅРѕСЃС‚Рё Рё СЂР°Р±РѕС‚Р° СЃ РµСЃС‚РµСЃС‚РІРµРЅРЅС‹Рј С‚РµРјРїРѕРј СЂРµС‡Рё РЅРѕСЃРёС‚РµР»РµР№.</p>
          <ul class="service-points">
            <li>РЈСЂРѕРІРЅРё: A2вЂ“C1</li>
            <li>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ: 12вЂ“20 РјРёРЅСѓС‚</li>
            <li>Р¤РѕРєСѓСЃ: РїРѕРЅРёРјР°РЅРёРµ Рё СЂР°Р·Р±РѕСЂ С„СЂР°Р·</li>
          </ul>
          <a class="button ghost" href="contacts.html">РџРћР›РЈР§РРўР¬ Р”РћРЎРўРЈРџ</a>
        </article>
        <article class="service-item detailed">
          <span class="meta-text">Р¤РћР РњРђРў 04</span>
          <h3>Р“СЂР°РјРјР°С‚РёС‡РµСЃРєРёРµ СѓРїСЂР°Р¶РЅРµРЅРёСЏ</h3>
          <p>РћС‚СЂР°Р±РѕС‚РєР° РїСЂР°РІРёР» РІ РїСЂРёРєР»Р°РґРЅРѕРј С„РѕСЂРјР°С‚Рµ Р±РµР· РїРµСЂРµРіСЂСѓР·РєРё С‚РµРѕСЂРёРµР№ Рё РґР»РёРЅРЅС‹РјРё С‚Р°Р±Р»РёС†Р°РјРё.</p>
          <ul class="service-points">
            <li>РўРµРјС‹: РІСЂРµРјРµРЅР°, РјРѕРґР°Р»СЊРЅС‹Рµ С„РѕСЂРјС‹, Р°СЂС‚РёРєР»Рё</li>
            <li>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ: 10вЂ“15 РјРёРЅСѓС‚</li>
            <li>Р¤РѕРєСѓСЃ: РїСЂРёРјРµРЅРµРЅРёРµ РІ РєРѕРЅС‚РµРєСЃС‚Рµ</li>
          </ul>
          <a class="button ghost" href="blog.html">РЈР—РќРђРўР¬ Р‘РћР›Р¬РЁР•</a>
        </article>
        <article class="service-item detailed">
          <span class="meta-text">Р¤РћР РњРђРў 05</span>
          <h3>РРЅРґРёРІРёРґСѓР°Р»СЊРЅР°СЏ С‚СЂР°РµРєС‚РѕСЂРёСЏ</h3>
          <p>РџРѕРґР±РѕСЂ СЏР·С‹РєРѕРІРѕРіРѕ РЅР°РїСЂР°РІР»РµРЅРёСЏ, РЅР°РіСЂСѓР·РєРё Рё РїСЂРёРѕСЂРёС‚РµС‚РЅС‹С… С‚РµРј РїРѕРґ РєРѕРЅРєСЂРµС‚РЅС‹Рµ С†РµР»Рё РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ.</p>
          <ul class="service-points">
            <li>Р¦РµР»Рё: РїСѓС‚РµС€РµСЃС‚РІРёСЏ, СЂР°Р±РѕС‚Р°, РїРµСЂРµРµР·Рґ</li>
            <li>РЎС‚Р°СЂС‚: РїРѕСЃР»Рµ РІС…РѕРґРЅРѕР№ Р·Р°СЏРІРєРё</li>
            <li>Р¤РѕРєСѓСЃ: РїРµСЂСЃРѕРЅР°Р»СЊРЅС‹Р№ РјР°СЂС€СЂСѓС‚</li>
          </ul>
          <a class="button solid" href="contacts.html">РћРЎРўРђР’РРўР¬ Р—РђРЇР’РљРЈ</a>
        </article>
        <article class="service-item detailed service-highlight">
          <span class="meta-text">РџРћР”РҐРћР”</span>
          <h3>РљР°Рє СЂРµР°Р»РёР·РѕРІР°РЅС‹ СѓСЃР»СѓРіРё РЅР° СЃР°Р№С‚Рµ</h3>
          <p>РџСѓР±Р»РёС‡РЅР°СЏ С‡Р°СЃС‚СЊ Р·РЅР°РєРѕРјРёС‚ СЃ С„РѕСЂРјР°С‚Р°РјРё РѕР±СѓС‡РµРЅРёСЏ, СѓСЂРѕРєРё РґР°СЋС‚ Р±С‹СЃС‚СЂС‹Р№ РІС…РѕРґ РІ РїСЂР°РєС‚РёРєСѓ, Р° С„РѕСЂРјР° РєРѕРЅС‚Р°РєС‚РѕРІ РїРѕР·РІРѕР»СЏРµС‚ Р·Р°РїСЂРѕСЃРёС‚СЊ РёРЅРґРёРІРёРґСѓР°Р»СЊРЅС‹Р№ РјР°СЂС€СЂСѓС‚.</p>
          <a class="button ghost" href="contacts.html">РЎР’РЇР—РђРўР¬РЎРЇ РЎ РќРђРњР</a>
        </article>
      </div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
</body>
</html>
```

## lessons.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РЈСЂРѕРєРё</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="lessons">
  <div id="site-header"></div>

  <main class="page">
    <section class="page-intro">
      <p class="eyebrow">РЈР РћРљР</p>
      <h1>РџРѕРґР±РѕСЂРєР° РєРѕСЂРѕС‚РєРёС… РїСЂР°РєС‚РёС‡РµСЃРєРёС… СѓСЂРѕРєРѕРІ</h1>
      <p class="lead">Р—РґРµСЃСЊ РјРѕР¶РЅРѕ РїСЂРѕСЃРјР°С‚СЂРёРІР°С‚СЊ СѓСЂРѕРєРё РґР»СЏ Р°РЅРіР»РёР№СЃРєРѕРіРѕ, РёСЃРїР°РЅСЃРєРѕРіРѕ, РЅРµРјРµС†РєРѕРіРѕ Рё С„СЂР°РЅС†СѓР·СЃРєРѕРіРѕ СЏР·С‹РєРѕРІ. РЎРѕР·РґР°РЅРёРµ Рё СѓРґР°Р»РµРЅРёРµ СѓСЂРѕРєРѕРІ РґРѕСЃС‚СѓРїРЅРѕ С‚РѕР»СЊРєРѕ Р°РІС‚РѕСЂРёР·РѕРІР°РЅРЅРѕРјСѓ СЃРѕС‚СЂСѓРґРЅРёРєСѓ.</p>
    </section>

    <section class="section-block hidden" id="lesson-admin-section">
      <div class="section-heading">
        <p class="eyebrow">Р”РћР‘РђР’Р›Р•РќРР• РЈР РћРљРђ</p>
        <h2>РЎРѕР·РґР°С‚СЊ РЅРѕРІС‹Р№ СѓСЂРѕРє</h2>
      </div>
      <form class="form-card" id="lesson-form" novalidate>
        <label>
          <span>РЇР·С‹Рє</span>
          <select id="lesson-language">
            <option value="">Р’С‹Р±РµСЂРёС‚Рµ СЏР·С‹Рє</option>
            <option value="РђРЅРіР»РёР№СЃРєРёР№">РђРЅРіР»РёР№СЃРєРёР№</option>
            <option value="РСЃРїР°РЅСЃРєРёР№">РСЃРїР°РЅСЃРєРёР№</option>
            <option value="РќРµРјРµС†РєРёР№">РќРµРјРµС†РєРёР№</option>
            <option value="Р¤СЂР°РЅС†СѓР·СЃРєРёР№">Р¤СЂР°РЅС†СѓР·СЃРєРёР№</option>
          </select>
        </label>
        <p class="field-error hidden" id="lesson-language-error">Р’С‹Р±РµСЂРёС‚Рµ СЏР·С‹Рє.</p>
        <label>
          <span>РЈСЂРѕРІРµРЅСЊ</span>
          <input id="lesson-level" type="text" placeholder="РќР°РїСЂРёРјРµСЂ: Beginner">
        </label>
        <p class="field-error hidden" id="lesson-level-error">РЈРєР°Р¶РёС‚Рµ СѓСЂРѕРІРµРЅСЊ РЅРµ РєРѕСЂРѕС‡Рµ 2 СЃРёРјРІРѕР»РѕРІ.</p>
        <label>
          <span>РќР°Р·РІР°РЅРёРµ СѓСЂРѕРєР°</span>
          <input id="lesson-title" type="text">
        </label>
        <p class="field-error hidden" id="lesson-title-error">РќР°Р·РІР°РЅРёРµ РґРѕР»Р¶РЅРѕ Р±С‹С‚СЊ РЅРµ РєРѕСЂРѕС‡Рµ 4 СЃРёРјРІРѕР»РѕРІ.</p>
        <label>
          <span>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ</span>
          <input id="lesson-duration" type="text" placeholder="РќР°РїСЂРёРјРµСЂ: 15 РјРёРЅСѓС‚">
        </label>
        <p class="field-error hidden" id="lesson-duration-error">РЈРєР°Р¶РёС‚Рµ РґР»РёС‚РµР»СЊРЅРѕСЃС‚СЊ.</p>
        <label>
          <span>Р¦РµР»СЊ СѓСЂРѕРєР°</span>
          <textarea id="lesson-goal" rows="4"></textarea>
        </label>
        <p class="field-error hidden" id="lesson-goal-error">Р¦РµР»СЊ РґРѕР»Р¶РЅР° Р±С‹С‚СЊ РЅРµ РєРѕСЂРѕС‡Рµ 10 СЃРёРјРІРѕР»РѕРІ.</p>
        <label>
          <span>Р—Р°РґР°РЅРёСЏ</span>
          <textarea id="lesson-tasks" rows="4" placeholder="РљР°Р¶РґРѕРµ Р·Р°РґР°РЅРёРµ СЃ РЅРѕРІРѕР№ СЃС‚СЂРѕРєРё"></textarea>
        </label>
        <p class="field-error hidden" id="lesson-tasks-error">Р”РѕР±Р°РІСЊС‚Рµ С…РѕС‚СЏ Р±С‹ 1 Р·Р°РґР°РЅРёРµ.</p>
        <div class="action-row form-actions">
          <button class="button solid" id="lesson-submit" type="submit" disabled>РЎРћР—Р”РђРўР¬ РЈР РћРљ</button>
        </div>
        <p class="form-note" id="lesson-status">Р—Р°РїРѕР»РЅРёС‚Рµ С„РѕСЂРјСѓ СѓСЂРѕРєР°.</p>
      </form>
      <div class="submission-card hidden" id="lesson-result"></div>
    </section>

    <section class="section-block hidden" id="lesson-access-note">
      <div class="submission-card">
        <p class="eyebrow">РћР“Р РђРќРР§Р•РќРР• Р”РћРЎРўРЈРџРђ</p>
        <h3>РЎРѕР·РґР°РЅРёРµ Рё СѓРґР°Р»РµРЅРёРµ СѓСЂРѕРєРѕРІ РґРѕСЃС‚СѓРїРЅРѕ С‚РѕР»СЊРєРѕ СЃРѕС‚СЂСѓРґРЅРёРєСѓ</h3>
        <p>РћР±С‹С‡РЅС‹Р№ РїРѕР»СЊР·РѕРІР°С‚РµР»СЊ РјРѕР¶РµС‚ РїСЂРѕСЃРјР°С‚СЂРёРІР°С‚СЊ РґРѕСЃС‚СѓРїРЅС‹Рµ СѓСЂРѕРєРё, РЅРѕ РЅРµ РјРѕР¶РµС‚ РёР·РјРµРЅСЏС‚СЊ СѓС‡РµР±РЅС‹Р№ РєРѕРЅС‚РµРЅС‚.</p>
      </div>
    </section>

    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">РЎРџРРЎРћРљ РЈР РћРљРћР’</p>
        <h2>Р’СЃРµ РґРѕСЃС‚СѓРїРЅС‹Рµ СѓСЂРѕРєРё</h2>
      </div>
      <div class="lessons-grid" id="lessons-list"></div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="lessons.js"></script>
</body>
</html>
```

## blog.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | Р‘Р»РѕРі</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="blog">
  <div id="site-header"></div>

  <main class="page">
    <section class="page-intro">
      <p class="eyebrow">Р‘Р›РћР“</p>
      <h1>РџСѓР±Р»РёРєР°С†РёРё Рѕ РїСЂР°РєС‚РёРєРµ СЏР·С‹РєР° Рё СѓС‡РµР±РЅС‹С… РїРѕРґС…РѕРґР°С…</h1>
      <p class="lead">Р—РґРµСЃСЊ СЃРѕР±СЂР°РЅС‹ РјР°С‚РµСЂРёР°Р»С‹ Рѕ С‚РѕРј, РєР°Рє РІС‹СЃС‚СЂР°РёРІР°С‚СЊ СЂРµРіСѓР»СЏСЂРЅС‹Рµ Р·Р°РЅСЏС‚РёСЏ, Р·Р°РєСЂРµРїР»СЏС‚СЊ Р»РµРєСЃРёРєСѓ Рё Р±С‹СЃС‚СЂРµРµ РїРµСЂРµС…РѕРґРёС‚СЊ Рє Р¶РёРІРѕРјСѓ РѕР±С‰РµРЅРёСЋ.</p>
    </section>

    <section class="section-block">
      <div class="blog-grid" id="blog-list"></div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="blog.js"></script>
</body>
</html>
```

## post.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РџСѓР±Р»РёРєР°С†РёСЏ</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="blog">
  <div id="site-header"></div>

  <main class="page narrow-page">
    <section class="section-block article-shell" id="post-shell"></section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="post.js"></script>
</body>
</html>
```

## contacts.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РљРѕРЅС‚Р°РєС‚С‹</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="contacts">
  <div id="site-header"></div>

  <main class="page">
    <section class="page-intro">
      <p class="eyebrow">РљРћРќРўРђРљРўР«</p>
      <h1>РЎРІСЏР¶РёС‚РµСЃСЊ СЃ РЅР°РјРё РґР»СЏ СЃС‚Р°СЂС‚Р° РѕР±СѓС‡РµРЅРёСЏ</h1>
      <p class="lead">РћСЃС‚Р°РІСЊС‚Рµ Р·Р°СЏРІРєСѓ, Рё РјС‹ РїРѕРґР±РµСЂРµРј СѓРґРѕР±РЅС‹Р№ С„РѕСЂРјР°С‚ РїСЂР°РєС‚РёРєРё Рё РЅСѓР¶РЅРѕРµ СЏР·С‹РєРѕРІРѕРµ РЅР°РїСЂР°РІР»РµРЅРёРµ.</p>
    </section>

    <section class="contact-layout">
      <div class="section-block">
        <div class="contact-info">
          <h2>РљРѕРЅС‚Р°РєС‚РЅР°СЏ РёРЅС„РѕСЂРјР°С†РёСЏ</h2>
          <ul class="plain-list">
            <li><strong>Р“СЂР°С„РёРє СЂР°Р±РѕС‚С‹:</strong> РџРЅвЂ“РџС‚, 10:00вЂ“19:00</li>
            <li><strong>РўРµР»РµС„РѕРЅ:</strong> +7 (812) 555-24-18</li>
            <li><strong>E-mail:</strong> hello@lingoflux.ru</li>
            <li><strong>РђРґСЂРµСЃ:</strong> РЎР°РЅРєС‚-РџРµС‚РµСЂР±СѓСЂРі, РќРµРІСЃРєРёР№ РїСЂ., 42</li>
          </ul>
        </div>
      </div>

      <section class="section-block">
        <form class="form-card" id="contact-form" novalidate>
          <label>
            <span>Р¤РРћ</span>
            <input id="contact-name" type="text" required>
          </label>
          <p class="field-error hidden" id="contact-name-error">Р’РІРµРґРёС‚Рµ Р¤РРћ РЅРµ РєРѕСЂРѕС‡Рµ 5 СЃРёРјРІРѕР»РѕРІ.</p>
          <label>
            <span>РљРѕРЅС‚Р°РєС‚РЅС‹Р№ РЅРѕРјРµСЂ С‚РµР»РµС„РѕРЅР°</span>
            <input id="contact-phone" type="tel" required>
          </label>
          <p class="field-error hidden" id="contact-phone-error">Р’РІРµРґРёС‚Рµ РєРѕСЂСЂРµРєС‚РЅС‹Р№ С‚РµР»РµС„РѕРЅ.</p>
          <label>
            <span>РђРґСЂРµСЃ СЌР»РµРєС‚СЂРѕРЅРЅРѕР№ РїРѕС‡С‚С‹</span>
            <input id="contact-email" type="email" required>
          </label>
          <p class="field-error hidden" id="contact-email-error">Р’РІРµРґРёС‚Рµ РєРѕСЂСЂРµРєС‚РЅС‹Р№ email.</p>
          <label>
            <span>РЎРѕРѕР±С‰РµРЅРёРµ</span>
            <textarea id="contact-message" rows="6" required></textarea>
          </label>
          <p class="field-error hidden" id="contact-message-error">РЎРѕРѕР±С‰РµРЅРёРµ РґРѕР»Р¶РЅРѕ Р±С‹С‚СЊ РЅРµ РєРѕСЂРѕС‡Рµ 10 СЃРёРјРІРѕР»РѕРІ.</p>
          <div class="action-row form-actions">
            <button class="button solid" id="contact-submit" type="submit" disabled>РћРўРџР РђР’РРўР¬ Р”РђРќРќР«Р•</button>
            <button class="button ghost" id="contact-preview-button" type="button">РџР Р•Р”РџР РћРЎРњРћРўР </button>
          </div>
          <p class="form-note" id="contact-status">Р—Р°РїРѕР»РЅРёС‚Рµ РІСЃРµ РїРѕР»СЏ С„РѕСЂРјС‹.</p>
        </form>

        <div class="submission-card hidden" id="contact-result"></div>
      </section>
    </section>
  </main>

  <div class="modal hidden" id="contact-preview-modal">
    <div class="modal-backdrop" data-close-contact-modal></div>
    <div class="modal-panel">
      <button class="modal-close" type="button" data-close-contact-modal>x</button>
      <div id="contact-preview-content"></div>
    </div>
  </div>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="contacts.js"></script>
</body>
</html>
```

## login.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РђРІС‚РѕСЂРёР·Р°С†РёСЏ</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="hidden">
  <div id="site-header"></div>

  <main class="page auth-page">
    <section class="section-block auth-block">
      <div class="section-heading">
        <p class="eyebrow">РЎРљР Р«РўР«Р™ Р РђР—Р”Р•Р›</p>
        <h1>РђРІС‚РѕСЂРёР·Р°С†РёСЏ СЃРѕС‚СЂСѓРґРЅРёРєР°</h1>
      </div>
      <form class="form-card compact-form" id="login-form">
        <label>
          <span>Р›РѕРіРёРЅ</span>
          <input id="login-name" type="text" autocomplete="username">
        </label>
        <label>
          <span>РџР°СЂРѕР»СЊ</span>
          <input id="login-password" type="password" autocomplete="current-password">
        </label>
        <button class="button solid" id="login-button" type="button">Р’РћР™РўР</button>
        <p class="form-note">Р”РµРјРѕ-РґРѕСЃС‚СѓРї: `admin` / `lingoflux2026`</p>
        <p class="form-note" id="login-status">РСЃРїРѕР»СЊР·СѓР№С‚Рµ СЃР»СѓР¶РµР±РЅСѓСЋ СѓС‡РµС‚РЅСѓСЋ Р·Р°РїРёСЃСЊ РґР»СЏ РґРѕСЃС‚СѓРїР° Рє Р·Р°РєСЂС‹С‚С‹Рј СЂР°Р·РґРµР»Р°Рј.</p>
      </form>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="login.js"></script>
</body>
</html>
```

## create-post.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | РЎРѕР·РґР°РЅРёРµ РїСѓР±Р»РёРєР°С†РёРё</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="create">
  <div id="site-header"></div>

  <main class="page">
    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">РЎРћР—Р”РђРќРР• РџРЈР‘Р›РРљРђР¦РР</p>
        <h1>Р”РѕР±Р°РІР»РµРЅРёРµ РјР°С‚РµСЂРёР°Р»Р° РІ Р±Р»РѕРі</h1>
      </div>
      <form class="form-card editor-form" id="post-form" novalidate>
        <label>
          <span>Р—Р°РіРѕР»РѕРІРѕРє СЃС‚Р°С‚СЊРё</span>
          <input id="post-title" type="text" required>
        </label>
        <p class="field-error hidden" id="post-title-error">Р—Р°РіРѕР»РѕРІРѕРє РґРѕР»Р¶РµРЅ Р±С‹С‚СЊ РЅРµ РєРѕСЂРѕС‡Рµ 4 СЃРёРјРІРѕР»РѕРІ.</p>
        <label>
          <span>РљСЂР°С‚РєРѕРµ РѕРїРёСЃР°РЅРёРµ</span>
          <input id="post-summary" type="text" placeholder="РљРѕСЂРѕС‚РєРёР№ Р°РЅРѕРЅСЃ РїСѓР±Р»РёРєР°С†РёРё">
        </label>
        <label>
          <span>РЎРѕРґРµСЂР¶Р°РЅРёРµ</span>
          <textarea id="post-body" rows="12" required placeholder="РџРѕРґРґРµСЂР¶РёРІР°РµС‚СЃСЏ РїСЂРѕСЃС‚Р°СЏ markdown-СЂР°Р·РјРµС‚РєР°: **Р¶РёСЂРЅС‹Р№**, *РєСѓСЂСЃРёРІ*, СЃРїРёСЃРєРё С‡РµСЂРµР· -"></textarea>
        </label>
        <p class="field-error hidden" id="post-body-error">РўРµРєСЃС‚ РїСѓР±Р»РёРєР°С†РёРё РґРѕР»Р¶РµРЅ Р±С‹С‚СЊ РЅРµ РєРѕСЂРѕС‡Рµ 20 СЃРёРјРІРѕР»РѕРІ.</p>
        <div class="file-row">
          <label class="button ghost file-button" for="post-file">Р”РћР‘РђР’РРўР¬ Р¤РђР™Р›</label>
          <input id="post-file" type="file" accept="image/*,.pdf,.doc,.docx,.txt" hidden>
          <span id="file-name">Р¤Р°Р№Р» РЅРµ РІС‹Р±СЂР°РЅ</span>
        </div>
        <div class="attachment-preview hidden" id="attachment-preview"></div>
        <div class="action-row form-actions">
          <button class="button ghost" id="preview-button" type="button">РџР Р•Р”РџР РћРЎРњРћРўР </button>
          <button class="button solid" id="publish-button" type="submit" disabled>РћРўРџР РђР’РРўР¬</button>
        </div>
        <p class="form-note" id="post-status">Р—Р°РїРѕР»РЅРёС‚Рµ Р·Р°РіРѕР»РѕРІРѕРє Рё СЃРѕРґРµСЂР¶Р°РЅРёРµ РїСѓР±Р»РёРєР°С†РёРё.</p>
      </form>

      <div class="submission-card hidden" id="post-result"></div>
    </section>

    <section class="section-block">
      <div class="section-heading">
        <p class="eyebrow">РЎРћРҐР РђРќР•РќРќР«Р• РџРЈР‘Р›РРљРђР¦РР</p>
        <h2>РўРµРєСѓС‰РёР№ СЃРїРёСЃРѕРє РїРѕСЃС‚РѕРІ</h2>
      </div>
      <div class="blog-grid" id="editor-post-list"></div>
    </section>
  </main>

  <div class="modal hidden" id="preview-modal">
    <div class="modal-backdrop" data-close-modal></div>
    <div class="modal-panel">
      <button class="modal-close" type="button" data-close-modal>x</button>
      <div id="preview-content"></div>
    </div>
  </div>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="create-post.js"></script>
</body>
</html>
```

## requests.html

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LingoFlux | Р—Р°СЏРІРєРё РїРѕР»СЊР·РѕРІР°С‚РµР»РµР№</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;700;800&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body data-page="requests">
  <div id="site-header"></div>

  <main class="page">
    <section class="page-intro">
      <p class="eyebrow">Р—РђРЇР’РљР РџРћР›Р¬Р—РћР’РђРўР•Р›Р•Р™</p>
      <h1>РЎРїРёСЃРѕРє РѕР±СЂР°С‰РµРЅРёР№ РёР· С„РѕСЂРјС‹ РѕР±СЂР°С‚РЅРѕР№ СЃРІСЏР·Рё</h1>
      <p class="lead">Р—РґРµСЃСЊ РѕС‚РѕР±СЂР°Р¶Р°СЋС‚СЃСЏ РІСЃРµ РґР°РЅРЅС‹Рµ, РѕС‚РїСЂР°РІР»РµРЅРЅС‹Рµ С‡РµСЂРµР· СЃС‚СЂР°РЅРёС†Сѓ РєРѕРЅС‚Р°РєС‚РѕРІ.</p>
    </section>

    <section class="section-block">
      <div class="request-grid" id="request-list"></div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="main.js"></script>
  <script src="data-service.js"></script>
  <script src="requests.js"></script>
</body>
</html>
```

## styles.css

```css
:root {
  --bg: #f5efe4;
  --paper: rgba(255, 249, 239, 0.88);
  --paper-strong: #fffaf3;
  --text: #1d1b18;
  --muted: #61574c;
  --line: rgba(29, 27, 24, 0.1);
  --accent: #d9622b;
  --accent-dark: #b54a1a;
  --teal: #0f7974;
  --danger: #b53d2d;
  --shadow: 0 24px 80px rgba(77, 54, 26, 0.12);
  --radius-xl: 32px;
  --radius-lg: 24px;
}
* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  margin: 0;
  font-family: "Manrope", sans-serif;
  color: var(--text);
  background:
    radial-gradient(circle at top left, rgba(217, 98, 43, 0.18), transparent 28%),
    radial-gradient(circle at 85% 18%, rgba(15, 121, 116, 0.18), transparent 22%),
    linear-gradient(180deg, #fffaf4 0%, var(--bg) 46%, #eddfc8 100%);
}
body::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  background-image:
    linear-gradient(rgba(255, 255, 255, 0.15) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.15) 1px, transparent 1px);
  background-size: 44px 44px;
  mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.5), transparent);
}
img { max-width: 100%; display: block; }
a { color: inherit; }
button, input, textarea, select { font: inherit; }
.page, .site-header-inner, .site-footer-inner { width: min(1180px, calc(100% - 28px)); margin: 0 auto; }
.page { padding: 116px 0 52px; }
.narrow-page { width: min(900px, calc(100% - 28px)); }
.site-header {
  position: fixed;
  inset: 0 0 auto;
  z-index: 30;
  backdrop-filter: blur(16px);
  background: rgba(255, 249, 240, 0.84);
  border-bottom: 1px solid var(--line);
}
.site-header-inner, .menu-links, .brand, .action-row, .pill-row, .file-row, .card-actions { display: flex; }
.site-header-inner {
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  min-height: 84px;
}
.brand { align-items: center; gap: 12px; text-decoration: none; }
.brand-mark {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
  border-radius: 14px;
  color: #fff;
  font-family: "Space Grotesk", sans-serif;
  font-weight: 700;
  background: linear-gradient(135deg, var(--accent), #eb8c45);
}
.brand-text, h1, h2, h3, .hero-card strong, .language-panel strong { font-family: "Space Grotesk", sans-serif; }
.brand-text { font-weight: 700; letter-spacing: 0.04em; }
.menu-links { align-items: center; gap: 26px; }
.menu-links a, .menu-links button.link-button {
  text-decoration: none;
  text-transform: uppercase;
  font-size: 0.88rem;
  font-weight: 800;
  letter-spacing: 0.08em;
  color: var(--text);
  background: none;
  border: 0;
  cursor: pointer;
  padding: 0;
}
.menu-links a.active { color: var(--teal); }
.header-controls { display: flex; align-items: center; gap: 12px; }
.menu-toggle {
  display: none;
  width: 44px;
  height: 44px;
  border-radius: 14px;
  border: 1px solid var(--line);
  background: rgba(255, 255, 255, 0.54);
  cursor: pointer;
}
.menu-toggle span, .menu-toggle::before, .menu-toggle::after {
  content: "";
  display: block;
  width: 18px;
  height: 2px;
  margin: 0 auto;
  background: var(--text);
  transition: transform 180ms ease, opacity 180ms ease;
}
.menu-toggle::before { transform: translateY(-5px); }
.menu-toggle::after { transform: translateY(5px); }
.menu-toggle.open::before { transform: translateY(2px) rotate(45deg); }
.menu-toggle.open span { opacity: 0; }
.menu-toggle.open::after { transform: translateY(-2px) rotate(-45deg); }
.hero-banner, .section-block, .hero-card, .feature-card, .service-item, .blog-card, .form-card, .contact-info, .request-card, .article-shell, .cta-panel, .lesson-card, .submission-card, .attachment-preview, .file-box {
  border: 1px solid var(--line);
  border-radius: var(--radius-xl);
  background: var(--paper);
  box-shadow: var(--shadow);
  backdrop-filter: blur(18px);
}
.hero-banner {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 24px;
  padding: 34px;
}
.hero-copy, .hero-visual { display: grid; gap: 18px; }
.hero-visual { align-content: start; }
.eyebrow {
  margin: 0;
  color: var(--teal);
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-size: 0.76rem;
  font-weight: 800;
}
h1 { margin: 0; font-size: clamp(2.7rem, 5vw, 5rem); line-height: 0.95; }
h2 { margin: 0; font-size: clamp(1.9rem, 3vw, 3rem); line-height: 1.02; }
h3 { margin: 0 0 10px; font-size: 1.3rem; }
.lead, .feature-card p, .hero-card p, .service-item p, .blog-card p, .form-note, .contact-info li, .article-body, .request-card p, .lesson-card p, .submission-card p, .attachment-preview p, .file-box p, .preview-stack p {
  color: var(--muted);
  line-height: 1.7;
}
.lead { font-size: 1.02rem; }
.lead.compact { margin: 0; }
.button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 999px;
  padding: 14px 22px;
  text-decoration: none;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  border: 1px solid transparent;
  cursor: pointer;
  transition: transform 180ms ease, background 180ms ease;
}
.button:hover { transform: translateY(-2px); }
.button.solid { background: var(--accent); color: #fff; }
.button.solid:hover { background: var(--accent-dark); }
.button.ghost { background: rgba(255, 255, 255, 0.48); border-color: var(--line); }
.button:disabled { cursor: not-allowed; opacity: 0.55; transform: none; }
.hero-card, .feature-card, .service-item, .blog-card, .request-card, .contact-info, .article-shell, .cta-panel, .form-card, .lesson-card, .submission-card, .attachment-preview, .file-box { padding: 24px; }
.hero-card.accent { background: linear-gradient(135deg, rgba(15, 121, 116, 0.18), rgba(255, 250, 243, 0.86)); }
.hero-card span, .meta-text, .meta-inline {
  font-size: 0.82rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--teal);
  font-weight: 800;
}
.hero-card strong { font-size: 1.7rem; }
.section-block { margin-top: 24px; padding: 30px; }
.section-block.tinted { background: linear-gradient(135deg, rgba(255, 255, 255, 0.7), rgba(255, 239, 223, 0.88)); }
.section-heading { display: grid; gap: 10px; margin-bottom: 20px; }
.card-grid, .blog-grid, .request-grid, .service-list, .service-grid, .lessons-grid { display: grid; gap: 18px; }
.card-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
.service-list, .request-grid, .lessons-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.blog-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
.service-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }
.language-wrap { display: grid; gap: 18px; }
.pill-row { flex-wrap: wrap; gap: 12px; }
.language-pill {
  border: 1px solid var(--line);
  background: rgba(255, 255, 255, 0.58);
  border-radius: 999px;
  padding: 13px 18px;
  font-weight: 800;
  letter-spacing: 0.04em;
  cursor: pointer;
}
.language-pill.active { background: var(--teal); border-color: transparent; color: #fff; }
.language-panel {
  padding: 24px;
  border-radius: var(--radius-lg);
  background: rgba(29, 27, 24, 0.05);
}
.language-panel strong { display: block; margin: 10px 0 6px; font-size: 1.6rem; }
.cta-panel { display: flex; justify-content: space-between; align-items: center; gap: 18px; }
.page-intro { display: grid; gap: 12px; margin-bottom: 12px; }
.contact-layout { display: grid; grid-template-columns: 0.9fr 1.1fr; gap: 24px; }
.plain-list, .service-points {
  padding-left: 20px;
  margin: 18px 0 0;
  display: grid;
  gap: 10px;
}
.plain-list { list-style: none; padding-left: 0; }
.form-card { display: grid; gap: 16px; }
.form-card label { display: grid; gap: 8px; }
.form-card span { font-weight: 700; }
.form-card input, .form-card textarea, .form-card select {
  width: 100%;
  padding: 14px 16px;
  border-radius: 16px;
  border: 1px solid rgba(29, 27, 24, 0.14);
  background: rgba(255, 255, 255, 0.82);
}
.field-error {
  margin: -6px 0 0;
  color: var(--danger);
  font-size: 0.9rem;
  font-weight: 700;
}
.compact-form { max-width: 520px; }
.auth-page { display: grid; align-items: center; min-height: 100vh; }
.auth-block { max-width: 700px; margin: 0 auto; }
.blog-card, .lesson-card { display: grid; gap: 12px; }
.blog-card { grid-template-rows: auto auto 1fr auto; }
.blog-thumb {
  aspect-ratio: 16 / 9;
  border-radius: 18px;
  object-fit: cover;
  background: linear-gradient(135deg, rgba(217, 98, 43, 0.16), rgba(15, 121, 116, 0.12));
}
.article-shell img, .article-shell .blog-thumb { margin: 18px 0; }
.article-body h1, .article-body h2, .article-body h3 { margin-top: 24px; font-size: 1.5rem; }
.article-body p, .article-body li { color: var(--muted); line-height: 1.8; }
.article-body ul { padding-left: 20px; }
.lesson-top { display: flex; justify-content: space-between; gap: 12px; align-items: center; }
.lesson-chip {
  padding: 8px 12px;
  border-radius: 999px;
  background: rgba(15, 121, 116, 0.1);
  color: var(--teal);
  font-size: 0.84rem;
  font-weight: 800;
}
.detailed { display: grid; align-content: start; }
.service-highlight { background: linear-gradient(135deg, rgba(15, 121, 116, 0.14), rgba(255, 248, 238, 0.92)); }
.form-actions, .card-actions { gap: 12px; flex-wrap: wrap; }
.preview-stack, .submission-card, .attachment-preview { display: grid; gap: 8px; }
.file-row { align-items: center; gap: 14px; flex-wrap: wrap; }
.file-button { width: fit-content; }
.modal.hidden, .hidden { display: none; }
.modal {
  position: fixed;
  inset: 0;
  z-index: 50;
}
.modal-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(11, 17, 20, 0.54);
}
.modal-panel {
  position: relative;
  width: min(760px, calc(100% - 24px));
  max-height: calc(100vh - 48px);
  overflow: auto;
  margin: 24px auto;
  padding: 26px;
  border-radius: 28px;
  background: var(--paper-strong);
  box-shadow: var(--shadow);
}
.modal-close {
  position: absolute;
  top: 14px;
  right: 14px;
  border: 0;
  background: rgba(29, 27, 24, 0.06);
  width: 40px;
  height: 40px;
  border-radius: 999px;
  font-size: 1.5rem;
  cursor: pointer;
}
.site-footer { padding: 0 0 34px; }
.site-footer-inner {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  padding: 24px 28px;
  border: 1px solid var(--line);
  border-radius: 28px;
  background: rgba(255, 248, 238, 0.84);
  box-shadow: var(--shadow);
}
.footer-note { color: var(--muted); line-height: 1.7; }
.empty-state {
  padding: 28px;
  border-radius: var(--radius-lg);
  background: rgba(29, 27, 24, 0.04);
  color: var(--muted);
}
@media (max-width: 980px) {
  .hero-banner, .contact-layout, .cta-panel, .site-footer-inner {
    grid-template-columns: 1fr;
    flex-direction: column;
    align-items: stretch;
  }
  .card-grid, .service-list, .blog-grid, .request-grid, .service-grid, .lessons-grid { grid-template-columns: 1fr 1fr; }
}
@media (max-width: 760px) {
  .menu-toggle { display: block; }
  .menu-links {
    position: absolute;
    left: 14px;
    right: 14px;
    top: calc(100% + 10px);
    display: none;
    flex-direction: column;
    align-items: flex-start;
    gap: 16px;
    padding: 20px;
    border-radius: 24px;
    background: rgba(255, 250, 244, 0.96);
    border: 1px solid var(--line);
    box-shadow: var(--shadow);
  }
  .menu-links.open { display: flex; }
  .hero-banner, .section-block { padding: 22px; }
  .hero-banner { grid-template-columns: 1fr; }
  .card-grid, .service-list, .blog-grid, .request-grid, .service-grid, .lessons-grid { grid-template-columns: 1fr; }
  .page { width: min(100% - 18px, 100%); padding-top: 102px; }
  .action-row, .file-row, .card-actions { flex-direction: column; align-items: stretch; }
}
```

## main.js

```js
const STORAGE_KEYS = {
  posts: "lingoflux-posts",
  requests: "lingoflux-requests",
  auth: "lingoflux-auth",
  lessons: "lingoflux-lessons",
  app: "lingoflux-app-store",
};

const AUTH_QUERY_VALUE = "granted";
const AUTH_COOKIE_NAME = "lingoflux_auth";
const DB_NAME = "lingoflux-nosql";
const DB_VERSION = 1;
const DB_STORE_NAME = "appState";
const DB_RECORD_KEY = "primary";

const DEFAULT_POSTS = [
  {
    id: "post-1",
    title: "РљР°Рє РїРµСЂРµР№С‚Рё РѕС‚ РёР·СѓС‡РµРЅРёСЏ СЃР»РѕРІ Рє СЂРµР°Р»СЊРЅРѕР№ СЂРµС‡Рё",
    body: "**Р“Р»Р°РІРЅР°СЏ РѕС€РёР±РєР°** РЅР°С‡РёРЅР°СЋС‰РёС… СЃРѕСЃС‚РѕРёС‚ РІ С‚РѕРј, С‡С‚Рѕ СЃР»РѕРІР° СѓС‡Р°С‚СЃСЏ РѕС‚РґРµР»СЊРЅРѕ РѕС‚ РєРѕРЅС‚РµРєСЃС‚Р°.\n\n- РСЃРїРѕР»СЊР·СѓР№С‚Рµ РєРѕСЂРѕС‚РєРёРµ РґРёР°Р»РѕРіРё.\n- РџРѕРІС‚РѕСЂСЏР№С‚Рµ Р»РµРєСЃРёРєСѓ РІ РіРѕС‚РѕРІС‹С… С„СЂР°Р·Р°С….\n- Р’РѕР·РІСЂР°С‰Р°Р№С‚РµСЃСЊ Рє СЃР»РѕРІР°Рј С‡РµСЂРµР· 2-3 РґРЅСЏ.\n\nРўР°Рє СЏР·С‹Рє Р±С‹СЃС‚СЂРµРµ СЃС‚Р°РЅРѕРІРёС‚СЃСЏ СЂР°Р±РѕС‡РёРј РёРЅСЃС‚СЂСѓРјРµРЅС‚РѕРј.",
    date: "2026-04-27",
    image: "",
  },
  {
    id: "post-2",
    title: "РџРѕС‡РµРјСѓ РєРѕСЂРѕС‚РєРёРµ Р·Р°РЅСЏС‚РёСЏ СЂР°Р±РѕС‚Р°СЋС‚ Р»СѓС‡С€Рµ РјР°СЂР°С„РѕРЅРѕРІ",
    body: "Р РµРіСѓР»СЏСЂРЅРѕСЃС‚СЊ РІР°Р¶РЅРµРµ СЂРµРґРєРёС… РёРЅС‚РµРЅСЃРёРІРѕРІ. **15 РјРёРЅСѓС‚ РІ РґРµРЅСЊ** РґР°СЋС‚ СѓСЃС‚РѕР№С‡РёРІСѓСЋ РїСЂРёРІС‹С‡РєСѓ Рё СѓРјРµРЅСЊС€Р°СЋС‚ РїРµСЂРµРіСЂСѓР·РєСѓ.\n\nРўР°РєР°СЏ РјРѕРґРµР»СЊ РѕСЃРѕР±РµРЅРЅРѕ СѓРґРѕР±РЅР° РґР»СЏ РІР·СЂРѕСЃР»С‹С… РїРѕР»СЊР·РѕРІР°С‚РµР»РµР№, СЃРѕРІРјРµС‰Р°СЋС‰РёС… РѕР±СѓС‡РµРЅРёРµ СЃ СЂР°Р±РѕС‚РѕР№.",
    date: "2026-04-24",
    image: "",
  },
  {
    id: "post-3",
    title: "РўСЂРё С€Р°РіР° РґР»СЏ РїСЂР°РєС‚РёРєРё Р°РЅРіР»РёР№СЃРєРѕРіРѕ РїРµСЂРµРґ РїРѕРµР·РґРєРѕР№",
    body: "РџРµСЂРµРґ РїСѓС‚РµС€РµСЃС‚РІРёРµРј РІР°Р¶РЅРѕ РїРѕС‚СЂРµРЅРёСЂРѕРІР°С‚СЊ **С‚РёРїРѕРІС‹Рµ РґРёР°Р»РѕРіРё**.\n\n- Р’РѕРїСЂРѕСЃС‹ РІ Р°СЌСЂРѕРїРѕСЂС‚Сѓ.\n- Р‘СЂРѕРЅРёСЂРѕРІР°РЅРёРµ Рё Р·Р°СЃРµР»РµРЅРёРµ.\n- Р—Р°РєР°Р· РµРґС‹ Рё СѓС‚РѕС‡РЅРµРЅРёРµ РјР°СЂС€СЂСѓС‚Р°.\n\nРўР°РєРѕР№ РЅР°Р±РѕСЂ РґР°РµС‚ Р±С‹СЃС‚СЂС‹Р№ РїСЂРёРєР»Р°РґРЅРѕР№ СЂРµР·СѓР»СЊС‚Р°С‚ Рё СЃРЅРёР¶Р°РµС‚ СЃС‚СЂРµСЃСЃ РІ РїРѕРµР·РґРєРµ.",
    date: "2026-04-22",
    image: "",
  },
  {
    id: "post-4",
    title: "РљР°Рє РЅРµ Р±СЂРѕСЃРёС‚СЊ СЏР·С‹Рє РїРѕСЃР»Рµ РїРµСЂРІРѕР№ РЅРµРґРµР»Рё",
    body: "РЎРµРєСЂРµС‚ РІ С‚РѕРј, С‡С‚РѕР±С‹ РЅРµ СЃС‚СЂРѕРёС‚СЊ РѕР±СѓС‡РµРЅРёРµ РІРѕРєСЂСѓРі Р±РѕР»СЊС€РёС… РѕР±РµС‰Р°РЅРёР№.\n\nР›СѓС‡С€Рµ Р·Р°С„РёРєСЃРёСЂРѕРІР°С‚СЊ **РѕРґРёРЅ РєРѕСЂРѕС‚РєРёР№ СЂРёС‚СѓР°Р»**: РѕС‚РєСЂС‹С‚СЊ СѓСЂРѕРє СѓС‚СЂРѕРј, РїСЂРѕР№С‚Рё РјРёРЅРё-СѓРїСЂР°Р¶РЅРµРЅРёРµ РґРЅРµРј РёР»Рё РїРѕРІС‚РѕСЂРёС‚СЊ Р»РµРєСЃРёРєСѓ РІРµС‡РµСЂРѕРј. РџРѕСЃС‚РѕСЏРЅСЃС‚РІРѕ РІР°Р¶РЅРµРµ РѕР±СЉРµРјР°.",
    date: "2026-04-20",
    image: "",
  },
];

const DEFAULT_LESSONS = [
  {
    id: "lesson-1",
    level: "Beginner",
    title: "Р—РЅР°РєРѕРјСЃС‚РІРѕ Рё РїРµСЂРІС‹Р№ small talk",
    duration: "12 РјРёРЅСѓС‚",
    language: "РђРЅРіР»РёР№СЃРєРёР№",
    goal: "РќР°СѓС‡РёС‚СЊСЃСЏ РїСЂРµРґСЃС‚Р°РІР»СЏС‚СЊСЃСЏ, Р·Р°РґР°РІР°С‚СЊ РїСЂРѕСЃС‚С‹Рµ РІРѕРїСЂРѕСЃС‹ Рё РїРѕРґРґРµСЂР¶РёРІР°С‚СЊ РєРѕСЂРѕС‚РєРёР№ РґРёР°Р»РѕРі.",
    tasks: ["РџСЂРѕС‡РёС‚Р°С‚СЊ 8 С„СЂР°Р·", "РџРѕРІС‚РѕСЂРёС‚СЊ 6 СЂРµРїР»РёРє", "РЎРѕР±СЂР°С‚СЊ РјРёРЅРё-РґРёР°Р»РѕРі"],
  },
  {
    id: "lesson-2",
    level: "Intermediate",
    title: "РџСѓС‚РµС€РµСЃС‚РІРёРµ: Р°СЌСЂРѕРїРѕСЂС‚ Рё СЂРµРіРёСЃС‚СЂР°С†РёСЏ",
    duration: "18 РјРёРЅСѓС‚",
    language: "РђРЅРіР»РёР№СЃРєРёР№",
    goal: "РћС‚СЂР°Р±РѕС‚Р°С‚СЊ Р»РµРєСЃРёРєСѓ Рё С‚РёРїРѕРІС‹Рµ РІРѕРїСЂРѕСЃС‹ РґР»СЏ РїРµСЂРµРјРµС‰РµРЅРёСЏ РїРѕ Р°СЌСЂРѕРїРѕСЂС‚Сѓ.",
    tasks: ["Р›РµРєСЃРёРєР° РїРѕ С‚РµРјРµ", "Р”РёР°Р»РѕРі РІ Р°СЌСЂРѕРїРѕСЂС‚Сѓ", "РџСЂРѕРІРµСЂРєР° РїРѕРЅРёРјР°РЅРёСЏ"],
  },
  {
    id: "lesson-3",
    level: "Beginner",
    title: "РСЃРїР°РЅСЃРєРёР№ РґР»СЏ РєР°С„Рµ Рё Р·Р°РєР°Р·РѕРІ",
    duration: "14 РјРёРЅСѓС‚",
    language: "РСЃРїР°РЅСЃРєРёР№",
    goal: "РќР°СѓС‡РёС‚СЊСЃСЏ Р·Р°РєР°Р·С‹РІР°С‚СЊ Р±Р»СЋРґР°, СѓС‚РѕС‡РЅСЏС‚СЊ СЃРѕСЃС‚Р°РІ Рё РІРµР¶Р»РёРІРѕ РѕР±С‰Р°С‚СЊСЃСЏ СЃ РїРµСЂСЃРѕРЅР°Р»РѕРј.",
    tasks: ["5 РєР»СЋС‡РµРІС‹С… РєРѕРЅСЃС‚СЂСѓРєС†РёР№", "РњРµРЅСЋ Рё РёРЅРіСЂРµРґРёРµРЅС‚С‹", "РџСЂР°РєС‚РёРєР° СЂРµРїР»РёРє"],
  },
  {
    id: "lesson-4",
    level: "Upper-Intermediate",
    title: "Р Р°Р±РѕС‡Р°СЏ РІСЃС‚СЂРµС‡Р° РЅР° РЅРµРјРµС†РєРѕРј",
    duration: "20 РјРёРЅСѓС‚",
    language: "РќРµРјРµС†РєРёР№",
    goal: "Р—Р°РєСЂРµРїРёС‚СЊ Р»РµРєСЃРёРєСѓ РґРµР»РѕРІРѕРіРѕ РѕР±С‰РµРЅРёСЏ Рё РІРµР¶Р»РёРІС‹Рµ С„РѕСЂРјСѓР»РёСЂРѕРІРєРё РЅР° РІСЃС‚СЂРµС‡Р°С….",
    tasks: ["Р¤СЂР°Р·С‹ РґР»СЏ РЅР°С‡Р°Р»Р° РІСЃС‚СЂРµС‡Рё", "РЎРѕРіР»Р°СЃРѕРІР°РЅРёРµ РґРµР№СЃС‚РІРёР№", "РС‚РѕРіРѕРІРѕРµ СѓРїСЂР°Р¶РЅРµРЅРёРµ"],
  },
];

const APP_DEFAULT_STATE = {
  posts: DEFAULT_POSTS,
  requests: [],
  lessons: DEFAULT_LESSONS,
  auth: false,
};

const storageBridge = (() => {
  const memoryStore = {};

  function readWindowName() {
    try {
      return window.name ? JSON.parse(window.name) : {};
    } catch (error) {
      return {};
    }
  }

  function writeWindowName(payload) {
    try {
      window.name = JSON.stringify(payload);
    } catch (error) {
      window.name = "";
    }
  }

  function canUseLocalStorage() {
    try {
      const probe = "__lingoflux_probe__";
      window.localStorage.setItem(probe, "1");
      window.localStorage.removeItem(probe);
      return true;
    } catch (error) {
      return false;
    }
  }

  const localStorageEnabled = canUseLocalStorage();

  function getItem(key) {
    if (localStorageEnabled) {
      return window.localStorage.getItem(key);
    }

    const payload = readWindowName();
    if (Object.prototype.hasOwnProperty.call(payload, key)) {
      return payload[key];
    }
    if (Object.prototype.hasOwnProperty.call(memoryStore, key)) {
      return memoryStore[key];
    }
    return null;
  }

  function setItem(key, value) {
    if (localStorageEnabled) {
      window.localStorage.setItem(key, value);
      return;
    }

    const payload = readWindowName();
    payload[key] = value;
    memoryStore[key] = value;
    writeWindowName(payload);
  }

  return {
    getItem,
    setItem,
    canUseLocalStorage,
  };
})();

function safeJsonParse(value, fallback) {
  try {
    return value ? JSON.parse(value) : fallback;
  } catch (error) {
    return fallback;
  }
}

function cloneDefaultState() {
  return JSON.parse(JSON.stringify(APP_DEFAULT_STATE));
}

function normalizeState(value) {
  return {
    posts: Array.isArray(value?.posts) ? value.posts : [...DEFAULT_POSTS],
    requests: Array.isArray(value?.requests) ? value.requests : [],
    lessons: Array.isArray(value?.lessons) ? value.lessons : [...DEFAULT_LESSONS],
    auth: value?.auth === true,
  };
}

function dispatchStoreEvent(name) {
  document.dispatchEvent(new CustomEvent(name, { detail: { source: "appState" } }));
}

function setCookieAuth(value) {
  try {
    document.cookie = `${AUTH_COOKIE_NAME}=${value ? "true" : "false"}; path=/; max-age=2592000`;
  } catch (error) {
    document.cookie = `${AUTH_COOKIE_NAME}=${value ? "true" : "false"}`;
  }
}

function getCookieAuth() {
  return document.cookie
    .split(";")
    .map((item) => item.trim())
    .some((item) => item === `${AUTH_COOKIE_NAME}=true`);
}

function hasAuthQuery() {
  const params = new URLSearchParams(window.location.search);
  return params.get("auth") === AUTH_QUERY_VALUE;
}

function writeWindowAuth(value) {
  const payload = safeJsonParse(storageBridge.getItem("__window_auth_state__"), {});
  payload.authorized = value;
  storageBridge.setItem("__window_auth_state__", JSON.stringify(payload));
}

function readWindowAuth() {
  const payload = safeJsonParse(storageBridge.getItem("__window_auth_state__"), {});
  return payload.authorized === true;
}

function readLegacyAuth() {
  return storageBridge.getItem(STORAGE_KEYS.auth) === "true";
}

function readLegacyState() {
  const migrated = cloneDefaultState();
  const legacyPosts = safeJsonParse(storageBridge.getItem(STORAGE_KEYS.posts), null);
  const legacyRequests = safeJsonParse(storageBridge.getItem(STORAGE_KEYS.requests), null);
  const legacyLessons = safeJsonParse(storageBridge.getItem(STORAGE_KEYS.lessons), null);

  if (Array.isArray(legacyPosts) && legacyPosts.length) {
    migrated.posts = legacyPosts;
  }
  if (Array.isArray(legacyRequests)) {
    migrated.requests = legacyRequests;
  }
  if (Array.isArray(legacyLessons) && legacyLessons.length) {
    migrated.lessons = legacyLessons;
  }
  migrated.auth = readLegacyAuth();
  return migrated;
}

function mirrorToLegacyStores(store) {
  storageBridge.setItem(STORAGE_KEYS.app, JSON.stringify(store));
  storageBridge.setItem(STORAGE_KEYS.posts, JSON.stringify(store.posts));
  storageBridge.setItem(STORAGE_KEYS.requests, JSON.stringify(store.requests));
  storageBridge.setItem(STORAGE_KEYS.lessons, JSON.stringify(store.lessons));
  storageBridge.setItem(STORAGE_KEYS.auth, store.auth ? "true" : "false");
}

function createDbClient() {
  if (!("indexedDB" in window)) {
    return {
      supported: false,
      async read() {
        return null;
      },
      async write() {
        return false;
      },
    };
  }

  function open() {
    return new Promise((resolve, reject) => {
      const request = window.indexedDB.open(DB_NAME, DB_VERSION);

      request.onupgradeneeded = () => {
        const db = request.result;
        if (!db.objectStoreNames.contains(DB_STORE_NAME)) {
          db.createObjectStore(DB_STORE_NAME, { keyPath: "id" });
        }
      };

      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }

  async function withStore(mode, callback) {
    const db = await open();
    return new Promise((resolve, reject) => {
      const transaction = db.transaction(DB_STORE_NAME, mode);
      const store = transaction.objectStore(DB_STORE_NAME);
      const request = callback(store);

      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
      transaction.oncomplete = () => db.close();
      transaction.onerror = () => {
        db.close();
        reject(transaction.error);
      };
    });
  }

  return {
    supported: true,
    async read() {
      const record = await withStore("readonly", (store) => store.get(DB_RECORD_KEY));
      return record?.value || null;
    },
    async write(value) {
      await withStore("readwrite", (store) => store.put({ id: DB_RECORD_KEY, value }));
      return true;
    },
  };
}

const dbClient = createDbClient();
let appStore = normalizeState(readLegacyState());

async function persistStore() {
  mirrorToLegacyStores(appStore);
  if (dbClient.supported) {
    try {
      await dbClient.write(appStore);
    } catch (error) {
      console.error("IndexedDB write error:", error);
    }
  }
  dispatchStoreEvent("app-store-updated");
}

async function hydrateStore() {
  mirrorToLegacyStores(appStore);
  if (!dbClient.supported) {
    dispatchStoreEvent("app-store-ready");
    return;
  }

  try {
    const dbState = await dbClient.read();
    if (dbState) {
      appStore = normalizeState(dbState);
      mirrorToLegacyStores(appStore);
    } else {
      await dbClient.write(appStore);
    }
  } catch (error) {
    console.error("IndexedDB read error:", error);
  }

  dispatchStoreEvent("app-store-ready");
}

function getPosts() {
  return [...appStore.posts];
}

function savePosts(posts) {
  appStore.posts = Array.isArray(posts) ? posts : [];
  void persistStore();
}

function getRequests() {
  return [...appStore.requests];
}

function saveRequests(items) {
  appStore.requests = Array.isArray(items) ? items : [];
  void persistStore();
}

function getLessons() {
  return [...appStore.lessons];
}

function saveLessons(items) {
  appStore.lessons = Array.isArray(items) ? items : [];
  void persistStore();
}

function isAuthorized() {
  if (hasAuthQuery()) {
    return true;
  }
  if (appStore.auth === true) {
    return true;
  }
  if (getCookieAuth()) {
    return true;
  }
  return readWindowAuth();
}

function setAuthorized(value) {
  appStore.auth = value === true;
  setCookieAuth(value);
  writeWindowAuth(value);
  void persistStore();
}

function formatDate(isoDate) {
  const date = new Date(isoDate);
  if (Number.isNaN(date.getTime())) {
    return isoDate;
  }
  return date.toLocaleDateString("ru-RU", {
    day: "2-digit",
    month: "long",
    year: "numeric",
  });
}

function inlineMarkdown(text) {
  return text.replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\*(.+?)\*/g, "<em>$1</em>");
}

function markdownToHtml(text) {
  const safe = String(text)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");

  return safe
    .split(/\n{2,}/)
    .map((block) => {
      if (block.startsWith("- ")) {
        const items = block
          .split("\n")
          .map((line) => line.replace(/^- /, "").trim())
          .filter(Boolean)
          .map((line) => `<li>${inlineMarkdown(line)}</li>`)
          .join("");
        return `<ul>${items}</ul>`;
      }

      return `<p>${inlineMarkdown(block.replace(/\n/g, "<br>"))}</p>`;
    })
    .join("");
}

function createHeader() {
  const page = document.body.dataset.page;
  const auth = isAuthorized();
  const navLinks = [
    { href: "index.html", label: "Р“Р›РђР’РќРђРЇ", key: "home" },
    { href: "services.html", label: "РЈРЎР›РЈР“Р", key: "services" },
    { href: "lessons.html", label: "РЈР РћРљР", key: "lessons" },
    { href: "blog.html", label: "Р‘Р›РћР“", key: "blog" },
    { href: "contacts.html", label: "РљРћРќРўРђРљРўР«", key: "contacts" },
  ];

  const hiddenLinks = auth
    ? `
      <a href="create-post.html?auth=${AUTH_QUERY_VALUE}" class="${location.pathname.endsWith("create-post.html") ? "active" : ""}">РџРЈР‘Р›РРљРђР¦РР</a>
      <a href="requests.html?auth=${AUTH_QUERY_VALUE}" class="${location.pathname.endsWith("requests.html") ? "active" : ""}">Р—РђРЇР’РљР</a>
    `
    : "";

  const authControl = auth
    ? `<button class="link-button" id="logout-button" type="button">Р’Р«РҐРћР”</button>`
    : `<a href="login.html">Р’РҐРћР”</a>`;

  return `
    <header class="site-header">
      <div class="site-header-inner">
        <a class="brand" href="index.html">
          <span class="brand-mark">LF</span>
          <span class="brand-text">LingoFlux</span>
        </a>
        <nav class="menu-links" id="menu-links">
          ${navLinks.map((link) => `<a href="${link.href}" class="${page === link.key ? "active" : ""}">${link.label}</a>`).join("")}
          ${hiddenLinks}
        </nav>
        <div class="header-controls">
          ${authControl}
          <button class="menu-toggle" id="menu-toggle" type="button" aria-label="РњРµРЅСЋ"><span></span></button>
        </div>
      </div>
    </header>
  `;
}

function createFooter() {
  return `
    <footer class="site-footer">
      <div class="site-footer-inner">
        <div>
          <strong>LingoFlux</strong>
          <p class="footer-note">РЎРµСЂРІРёСЃ РїСЂР°РєС‚РёРєРё РёРЅРѕСЃС‚СЂР°РЅРЅС‹С… СЏР·С‹РєРѕРІ СЃ Р°РєС†РµРЅС‚РѕРј РЅР° СЂРµРіСѓР»СЏСЂРЅС‹Рµ РєРѕСЂРѕС‚РєРёРµ Р·Р°РЅСЏС‚РёСЏ, Р¶РёРІС‹Рµ СЃС†РµРЅР°СЂРёРё РѕР±С‰РµРЅРёСЏ Рё СѓРґРѕР±РЅСѓСЋ С‚СЂР°РµРєС‚РѕСЂРёСЋ РѕР±СѓС‡РµРЅРёСЏ.</p>
        </div>
        <div>
          <strong>РљРѕРЅС‚Р°РєС‚С‹</strong>
          <p class="footer-note">РџРЅ-РџС‚, 10:00-19:00<br>+7 (812) 555-24-18<br>hello@lingoflux.ru</p>
        </div>
      </div>
    </footer>
  `;
}

function renderShell() {
  const header = document.getElementById("site-header");
  const footer = document.getElementById("site-footer");

  if (header) {
    header.innerHTML = createHeader();
  }
  if (footer) {
    footer.innerHTML = createFooter();
  }

  const toggle = document.getElementById("menu-toggle");
  const menu = document.getElementById("menu-links");

  if (toggle && menu) {
    toggle.addEventListener("click", () => {
      toggle.classList.toggle("open");
      menu.classList.toggle("open");
    });
  }

  const logout = document.getElementById("logout-button");
  if (logout) {
    logout.addEventListener("click", () => {
      setAuthorized(false);
      window.location.href = "index.html";
    });
  }
}

function protectHiddenPage() {
  if (document.body.dataset.page !== "hidden") {
    return;
  }

  if (!isAuthorized() && !location.pathname.endsWith("login.html")) {
    window.location.href = "login.html";
  }
}

protectHiddenPage();
renderShell();
const appStoreReady = hydrateStore();

window.appState = {
  getPosts,
  savePosts,
  getRequests,
  saveRequests,
  getLessons,
  saveLessons,
  isAuthorized,
  setAuthorized,
  formatDate,
  markdownToHtml,
  authQueryValue: AUTH_QUERY_VALUE,
  ready: appStoreReady,
  storageMode: dbClient.supported ? "indexedDB" : (storageBridge.canUseLocalStorage() ? "localStorage" : "window.name"),
};
```

## data-service.js

```js
(function () {
  const state = window.appState;

  function ensureState() {
    if (!state) {
      throw new Error("РЎРµСЂРІРёСЃ РґР°РЅРЅС‹С… РЅРµРґРѕСЃС‚СѓРїРµРЅ: appState РЅРµ РёРЅРёС†РёР°Р»РёР·РёСЂРѕРІР°РЅ.");
    }
    return state;
  }

  async function waitReady() {
    const current = ensureState();
    if (current.ready && typeof current.ready.then === "function") {
      await current.ready;
    }
    return current;
  }

  function sortByDateDesc(items) {
    return [...items].sort((a, b) => new Date(b.date) - new Date(a.date));
  }

  function createId(prefix) {
    return `${prefix}-${Date.now()}`;
  }

  window.appService = {
    async ready() {
      return waitReady();
    },
    posts: {
      async list() {
        const current = await waitReady();
        return sortByDateDesc(current.getPosts());
      },
      async getById(id) {
        const current = await waitReady();
        return current.getPosts().find((item) => item.id === id) || null;
      },
      async create(payload) {
        const current = await waitReady();
        const posts = current.getPosts();
        const created = {
          id: createId("post"),
          title: payload.title,
          summary: payload.summary || "",
          body: payload.body,
          date: new Date().toISOString(),
          image: payload.image || "",
          attachment: payload.attachment || null,
        };
        posts.unshift(created);
        current.savePosts(posts);
        return created;
      },
      async remove(id) {
        const current = await waitReady();
        const next = current.getPosts().filter((item) => item.id !== id);
        current.savePosts(next);
        return true;
      },
    },
    requests: {
      async list() {
        const current = await waitReady();
        return sortByDateDesc(current.getRequests());
      },
      async create(payload) {
        const current = await waitReady();
        const requests = current.getRequests();
        const created = {
          id: createId("request"),
          fullname: payload.fullname,
          phone: payload.phone,
          email: payload.email,
          message: payload.message,
          date: new Date().toISOString(),
        };
        requests.unshift(created);
        current.saveRequests(requests);
        return created;
      },
      async remove(id) {
        const current = await waitReady();
        const next = current.getRequests().filter((item) => item.id !== id);
        current.saveRequests(next);
        return true;
      },
    },
    lessons: {
      async list() {
        const current = await waitReady();
        return current.getLessons();
      },
      async create(payload) {
        const current = await waitReady();
        const lessons = current.getLessons();
        const created = {
          id: createId("lesson"),
          language: payload.language,
          level: payload.level,
          title: payload.title,
          duration: payload.duration,
          goal: payload.goal,
          tasks: payload.tasks,
        };
        lessons.unshift(created);
        current.saveLessons(lessons);
        return created;
      },
      async remove(id) {
        const current = await waitReady();
        const next = current.getLessons().filter((item) => item.id !== id);
        current.saveLessons(next);
        return true;
      },
    },
    auth: {
      async login(login, password) {
        const current = await waitReady();
        const normalized = String(login || "").trim().toLowerCase();
        const rawPassword = String(password || "").trim();
        const success = (normalized === "admin" || normalized === "lingoflux")
          && (rawPassword === "lingoflux2026" || rawPassword === "admin2026");

        if (success) {
          current.setAuthorized(true);
        }
        return success;
      },
      async logout() {
        const current = await waitReady();
        current.setAuthorized(false);
      },
      async status() {
        const current = await waitReady();
        return current.isAuthorized();
      },
    },
  };
})();
```

## home.js

```js
const languageButtons = document.querySelectorAll(".language-pill");
const languageOutput = document.getElementById("language-output");

if (languageButtons.length && languageOutput) {
  languageButtons.forEach((button) => {
    button.addEventListener("click", () => {
      languageButtons.forEach((item) => item.classList.remove("active"));
      button.classList.add("active");
      languageOutput.textContent = button.dataset.language;
    });
  });
}
```

## blog.js

```js
const blogState = window.appState || {
  formatDate(value) {
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString("ru-RU");
  },
};

const blogList = document.getElementById("blog-list");

function getExcerpt(post) {
  const source = post.summary || post.body || "";
  return source.replace(/[*#-]/g, "").slice(0, 150);
}

async function renderBlog() {
  if (!blogList) {
    return;
  }

  const posts = window.appService ? await window.appService.posts.list() : [];

  blogList.innerHTML = posts.length
    ? posts.map((post) => `
      <article class="blog-card" data-post-id="${post.id}">
        ${post.image ? `<img class="blog-thumb" src="${post.image}" alt="${post.title}">` : '<div class="blog-thumb"></div>'}
        <span class="meta-text">${blogState.formatDate(post.date)}</span>
        <div>
          <h3>${post.title}</h3>
          <p>${getExcerpt(post)}...</p>
          ${post.attachment ? `<p class="meta-inline">Р•СЃС‚СЊ РІР»РѕР¶РµРЅРёРµ: ${post.attachment.name}</p>` : ""}
        </div>
        <div class="card-actions">
          <a class="button ghost" href="post.html?id=${post.id}">Р§РРўРђРўР¬ РџРћР›РќРћРЎРўР¬Р®</a>
          <button class="button ghost delete-post" type="button">РЈР”РђР›РРўР¬</button>
        </div>
      </article>
    `).join("")
    : '<div class="empty-state">РџСѓР±Р»РёРєР°С†РёР№ РїРѕРєР° РЅРµС‚.</div>';

  document.querySelectorAll(".delete-post").forEach((button) => {
    button.addEventListener("click", async () => {
      const card = button.closest("[data-post-id]");
      const postId = card?.dataset.postId;
      if (!postId) {
        return;
      }
      if (!window.confirm("РЈРґР°Р»РёС‚СЊ РїСѓР±Р»РёРєР°С†РёСЋ?")) {
        return;
      }
      if (window.appService) {
        await window.appService.posts.remove(postId);
      }
      await renderBlog();
    });
  });
}

void renderBlog();

document.addEventListener("app-store-ready", () => void renderBlog());
document.addEventListener("app-store-updated", () => void renderBlog());
```

## post.js

```js
const postPageState = window.appState || {
  formatDate(value) {
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString("ru-RU");
  },
  markdownToHtml(text) {
    const safe = String(text)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;");

    return safe
      .split(/\n{2,}/)
      .map((block) => `<p>${block.replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\*(.+?)\*/g, "<em>$1</em>").replace(/\n/g, "<br>")}</p>`)
      .join("");
  },
};

const postShell = document.getElementById("post-shell");
const params = new URLSearchParams(window.location.search);
const postId = params.get("id");

function renderAttachment(post) {
  if (!post.attachment) {
    return "";
  }
  const isImage = post.attachment.type && post.attachment.type.startsWith("image/");
  if (isImage && post.image) {
    return "";
  }
  return `
    <div class="file-box">
      <p><strong>Р’Р»РѕР¶РµРЅРёРµ:</strong> ${post.attachment.name}</p>
      <a class="button ghost" href="${post.attachment.data}" download="${post.attachment.name}">РЎРљРђР§РђРўР¬ Р¤РђР™Р›</a>
    </div>
  `;
}

async function renderPostPage() {
  if (!postShell) {
    return;
  }

  const post = window.appService ? await window.appService.posts.getById(postId) : null;

  postShell.innerHTML = post
    ? `
      <p class="eyebrow">РџРЈР‘Р›РРљРђР¦РРЇ</p>
      <h1>${post.title}</h1>
      <p class="meta-text">${postPageState.formatDate(post.date)}</p>
      ${post.summary ? `<p class="lead compact">${post.summary}</p>` : ""}
      ${post.image ? `<img class="blog-thumb" src="${post.image}" alt="${post.title}">` : ""}
      ${renderAttachment(post)}
      <div class="article-body">${postPageState.markdownToHtml(post.body)}</div>
    `
    : '<div class="empty-state">РџСѓР±Р»РёРєР°С†РёСЏ РЅРµ РЅР°Р№РґРµРЅР°.</div>';
}

void renderPostPage();

document.addEventListener("app-store-ready", () => void renderPostPage());
document.addEventListener("app-store-updated", () => void renderPostPage());
```

## contacts.js

```js
const contactAppState = window.appState || {
  formatDate(value) {
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString("ru-RU");
  },
};

const contactForm = document.getElementById("contact-form");
const contactStatus = document.getElementById("contact-status");
const contactSubmit = document.getElementById("contact-submit");
const contactPreviewButton = document.getElementById("contact-preview-button");
const contactResult = document.getElementById("contact-result");
const contactPreviewModal = document.getElementById("contact-preview-modal");
const contactPreviewContent = document.getElementById("contact-preview-content");
const contactFields = {
  fullname: document.getElementById("contact-name"),
  phone: document.getElementById("contact-phone"),
  email: document.getElementById("contact-email"),
  message: document.getElementById("contact-message"),
};
const contactErrors = {
  fullname: document.getElementById("contact-name-error"),
  phone: document.getElementById("contact-phone-error"),
  email: document.getElementById("contact-email-error"),
  message: document.getElementById("contact-message-error"),
};

function getContactPayload() {
  return {
    fullname: (contactFields.fullname?.value || "").trim(),
    phone: (contactFields.phone?.value || "").trim(),
    email: (contactFields.email?.value || "").trim(),
    message: (contactFields.message?.value || "").trim(),
  };
}

function validateEmail(value) {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
}

function validatePhone(value) {
  return /^[+\d][\d\s\-()]{5,}$/.test(value);
}

function setErrorState(element, show) {
  if (!element) {
    return;
  }
  element.classList.toggle("hidden", !show);
}

function validateContactForm() {
  const payload = getContactPayload();
  const checks = {
    fullname: payload.fullname.length >= 5,
    phone: validatePhone(payload.phone),
    email: validateEmail(payload.email),
    message: payload.message.length >= 10,
  };

  setErrorState(contactErrors.fullname, !checks.fullname && payload.fullname.length > 0);
  setErrorState(contactErrors.phone, !checks.phone && payload.phone.length > 0);
  setErrorState(contactErrors.email, !checks.email && payload.email.length > 0);
  setErrorState(contactErrors.message, !checks.message && payload.message.length > 0);

  const valid = Object.values(checks).every(Boolean);

  if (contactSubmit) {
    contactSubmit.disabled = !valid;
  }

  if (contactStatus) {
    if (!payload.fullname && !payload.phone && !payload.email && !payload.message) {
      contactStatus.textContent = "Р—Р°РїРѕР»РЅРёС‚Рµ РІСЃРµ РїРѕР»СЏ С„РѕСЂРјС‹.";
    } else if (!valid) {
      contactStatus.textContent = "РСЃРїСЂР°РІСЊС‚Рµ РѕС€РёР±РєРё РІ С„РѕСЂРјРµ.";
    } else {
      contactStatus.textContent = "Р¤РѕСЂРјР° РіРѕС‚РѕРІР° Рє РѕС‚РїСЂР°РІРєРµ.";
    }
  }

  return valid;
}

function renderContactPreview(payload) {
  if (!contactPreviewContent || !contactPreviewModal) {
    return;
  }

  contactPreviewContent.innerHTML = `
    <p class="eyebrow">РџР Р•Р”РџР РћРЎРњРћРўР  Р—РђРЇР’РљР</p>
    <h2>${payload.fullname || "Р‘РµР· РёРјРµРЅРё"}</h2>
    <div class="preview-stack">
      <p><strong>РўРµР»РµС„РѕРЅ:</strong> ${payload.phone || "РќРµ СѓРєР°Р·Р°РЅ"}</p>
      <p><strong>Email:</strong> ${payload.email || "РќРµ СѓРєР°Р·Р°РЅ"}</p>
      <p><strong>РЎРѕРѕР±С‰РµРЅРёРµ:</strong> ${payload.message || "РџСѓСЃС‚Рѕ"}</p>
    </div>
  `;
  contactPreviewModal.classList.remove("hidden");
}

function closeContactPreview() {
  if (contactPreviewModal) {
    contactPreviewModal.classList.add("hidden");
  }
}

Object.values(contactFields).forEach((field) => {
  if (field) {
    field.addEventListener("input", validateContactForm);
    field.addEventListener("change", validateContactForm);
    field.addEventListener("blur", validateContactForm);
  }
});

document.querySelectorAll("[data-close-contact-modal]").forEach((element) => {
  element.addEventListener("click", closeContactPreview);
});

if (contactPreviewButton) {
  contactPreviewButton.addEventListener("click", () => {
    renderContactPreview(getContactPayload());
  });
}

if (contactForm) {
  validateContactForm();

  contactForm.addEventListener("submit", async (event) => {
    event.preventDefault();

    if (!validateContactForm() || !window.appService) {
      return;
    }

    const payload = getContactPayload();
    const request = await window.appService.requests.create(payload);

    if (contactResult) {
      contactResult.classList.remove("hidden");
      contactResult.innerHTML = `
        <p class="eyebrow">Р—РђРЇР’РљРђ РџР РРќРЇРўРђ</p>
        <h3>${payload.fullname}</h3>
        <p><strong>Р”Р°С‚Р°:</strong> ${contactAppState.formatDate(request.date)}</p>
        <p><strong>РўРµР»РµС„РѕРЅ:</strong> ${payload.phone}</p>
        <p><strong>Email:</strong> ${payload.email}</p>
        <p><strong>РЎРѕРѕР±С‰РµРЅРёРµ:</strong> ${payload.message}</p>
      `;
    }

    contactForm.reset();
    Object.values(contactErrors).forEach((element) => setErrorState(element, false));
    contactStatus.textContent = "Р”Р°РЅРЅС‹Рµ РѕС‚РїСЂР°РІР»РµРЅС‹ Рё СЃРѕС…СЂР°РЅРµРЅС‹ С‡РµСЂРµР· СЃРµСЂРІРёСЃ.";
    validateContactForm();
  });
}
```

## login.js

```js
(function () {
  const loginForm = document.getElementById("login-form");
  const loginButton = document.getElementById("login-button");
  const loginStatus = document.getElementById("login-status");
  const loginField = document.getElementById("login-name");
  const passwordField = document.getElementById("login-password");

  function redirectToHiddenPage() {
    const target = new URL("create-post.html?auth=granted", window.location.href).toString();
    window.location.assign(target);
  }

  async function authorize() {
    const login = (loginField?.value || "").trim();
    const password = (passwordField?.value || "").trim();

    if (!window.appService) {
      if (loginStatus) {
        loginStatus.textContent = "РЎРµСЂРІРёСЃ Р°РІС‚РѕСЂРёР·Р°С†РёРё РЅРµРґРѕСЃС‚СѓРїРµРЅ.";
      }
      return;
    }

    const success = await window.appService.auth.login(login, password);
    if (success) {
      if (loginStatus) {
        loginStatus.textContent = "РђРІС‚РѕСЂРёР·Р°С†РёСЏ СѓСЃРїРµС€РЅР°. Р’С‹РїРѕР»РЅСЏРµС‚СЃСЏ РїРµСЂРµС…РѕРґ...";
      }
      redirectToHiddenPage();
      return;
    }

    if (loginStatus) {
      loginStatus.textContent = "РќРµРІРµСЂРЅС‹Р№ Р»РѕРіРёРЅ РёР»Рё РїР°СЂРѕР»СЊ.";
    }
  }

  if (loginButton) {
    loginButton.addEventListener("click", () => {
      void authorize();
    });
  }

  if (loginForm) {
    loginForm.addEventListener("submit", (event) => {
      event.preventDefault();
      void authorize();
    });
  }

  if (passwordField) {
    passwordField.addEventListener("keydown", (event) => {
      if (event.key === "Enter") {
        event.preventDefault();
        void authorize();
      }
    });
  }
})();
```

## create-post.js

```js
const postAppState = window.appState || {
  formatDate(value) {
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString("ru-RU");
  },
  markdownToHtml(text) {
    const safe = String(text)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;");

    return safe
      .split(/\n{2,}/)
      .map((block) => `<p>${block.replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\*(.+?)\*/g, "<em>$1</em>").replace(/\n/g, "<br>")}</p>`)
      .join("");
  },
};

const postForm = document.getElementById("post-form");
const postTitle = document.getElementById("post-title");
const postSummary = document.getElementById("post-summary");
const postBody = document.getElementById("post-body");
const postFile = document.getElementById("post-file");
const fileName = document.getElementById("file-name");
const attachmentPreview = document.getElementById("attachment-preview");
const publishButton = document.getElementById("publish-button");
const previewButton = document.getElementById("preview-button");
const postStatus = document.getElementById("post-status");
const postResult = document.getElementById("post-result");
const previewModal = document.getElementById("preview-modal");
const previewContent = document.getElementById("preview-content");
const titleError = document.getElementById("post-title-error");
const bodyError = document.getElementById("post-body-error");
const editorPostList = document.getElementById("editor-post-list");

let fileData = "";
let fileMeta = null;

function getPostPayload() {
  return {
    title: (postTitle?.value || "").trim(),
    summary: (postSummary?.value || "").trim(),
    body: (postBody?.value || "").trim(),
  };
}

function setErrorState(element, show) {
  if (!element) {
    return;
  }
  element.classList.toggle("hidden", !show);
}

function validatePostForm() {
  const payload = getPostPayload();
  const titleValid = payload.title.length >= 4;
  const bodyValid = payload.body.length >= 20;
  const valid = titleValid && bodyValid;

  setErrorState(titleError, !titleValid && payload.title.length > 0);
  setErrorState(bodyError, !bodyValid && payload.body.length > 0);

  if (publishButton) {
    publishButton.disabled = !valid;
  }

  if (postStatus) {
    if (!payload.title && !payload.body) {
      postStatus.textContent = "Р—Р°РїРѕР»РЅРёС‚Рµ Р·Р°РіРѕР»РѕРІРѕРє Рё СЃРѕРґРµСЂР¶Р°РЅРёРµ РїСѓР±Р»РёРєР°С†РёРё.";
    } else if (!valid) {
      postStatus.textContent = "РСЃРїСЂР°РІСЊС‚Рµ РѕС€РёР±РєРё РІ С„РѕСЂРјРµ РїСѓР±Р»РёРєР°С†РёРё.";
    } else {
      postStatus.textContent = "РџСѓР±Р»РёРєР°С†РёСЏ РіРѕС‚РѕРІР° Рє РѕС‚РїСЂР°РІРєРµ.";
    }
  }

  return valid;
}

function renderAttachmentCard() {
  if (!attachmentPreview) {
    return;
  }

  if (!fileMeta) {
    attachmentPreview.classList.add("hidden");
    attachmentPreview.innerHTML = "";
    return;
  }

  attachmentPreview.classList.remove("hidden");
  attachmentPreview.innerHTML = `
    <p class="eyebrow">РџР РРљР Р•РџР›Р•РќРР•</p>
    <p><strong>Р¤Р°Р№Р»:</strong> ${fileMeta.name}</p>
    <p><strong>РўРёРї:</strong> ${fileMeta.type || "РЅРµ СѓРєР°Р·Р°РЅ"}</p>
    <p><strong>Р Р°Р·РјРµСЂ:</strong> ${Math.ceil(fileMeta.size / 1024)} РљР‘</p>
    ${fileMeta.type && fileMeta.type.startsWith("image/") ? `<img class="blog-thumb" src="${fileData}" alt="${fileMeta.name}">` : ""}
  `;
}

function buildAttachmentMarkup() {
  if (!fileMeta) {
    return "";
  }

  const isImage = fileMeta.type && fileMeta.type.startsWith("image/");
  if (isImage) {
    return `<img class="blog-thumb" src="${fileData}" alt="${fileMeta.name}">`;
  }

  return `
    <div class="file-box">
      <p><strong>РџСЂРёРєСЂРµРїР»РµРЅРЅС‹Р№ С„Р°Р№Р»:</strong> ${fileMeta.name}</p>
      <a class="button ghost" href="${fileData}" download="${fileMeta.name}">РЎРљРђР§РђРўР¬ Р¤РђР™Р›</a>
    </div>
  `;
}

async function renderEditorPosts() {
  if (!editorPostList) {
    return;
  }

  const posts = window.appService ? await window.appService.posts.list() : [];
  editorPostList.innerHTML = posts.length
    ? posts.map((post) => `
      <article class="blog-card" data-editor-post-id="${post.id}">
        ${post.image ? `<img class="blog-thumb" src="${post.image}" alt="${post.title}">` : '<div class="blog-thumb"></div>'}
        <span class="meta-text">${postAppState.formatDate(post.date)}</span>
        <div>
          <h3>${post.title}</h3>
          <p>${(post.summary || post.body || "").replace(/[*#-]/g, "").slice(0, 140)}...</p>
          ${post.attachment ? `<p class="meta-inline">Р’Р»РѕР¶РµРЅРёРµ: ${post.attachment.name}</p>` : ""}
        </div>
        <div class="card-actions">
          <a class="button ghost" href="post.html?id=${post.id}">РћРўРљР Р«РўР¬</a>
          <button class="button ghost delete-editor-post" type="button">РЈР”РђР›РРўР¬</button>
        </div>
      </article>
    `).join("")
    : '<div class="empty-state">РџСѓР±Р»РёРєР°С†РёР№ РїРѕРєР° РЅРµС‚.</div>';

  document.querySelectorAll(".delete-editor-post").forEach((button) => {
    button.addEventListener("click", async () => {
      const card = button.closest("[data-editor-post-id]");
      const postId = card?.dataset.editorPostId;
      if (!postId || !window.confirm("РЈРґР°Р»РёС‚СЊ РїСѓР±Р»РёРєР°С†РёСЋ?")) {
        return;
      }
      if (window.appService) {
        await window.appService.posts.remove(postId);
      }
      await renderEditorPosts();
    });
  });
}

function openPreview() {
  const payload = getPostPayload();

  if (!previewContent || !previewModal) {
    return;
  }

  previewContent.innerHTML = `
    <p class="eyebrow">РџР Р•Р”РџР РћРЎРњРћРўР </p>
    <h2>${payload.title || "Р‘РµР· Р·Р°РіРѕР»РѕРІРєР°"}</h2>
    ${payload.summary ? `<p class="lead compact">${payload.summary}</p>` : ""}
    ${buildAttachmentMarkup()}
    <div class="article-body">${postAppState.markdownToHtml(payload.body || "")}</div>
  `;
  previewModal.classList.remove("hidden");
}

function closePreview() {
  if (previewModal) {
    previewModal.classList.add("hidden");
  }
}

[postTitle, postSummary, postBody].forEach((field) => {
  if (field) {
    field.addEventListener("input", validatePostForm);
    field.addEventListener("change", validatePostForm);
    field.addEventListener("blur", validatePostForm);
  }
});

if (postFile) {
  postFile.addEventListener("change", () => {
    const file = postFile.files && postFile.files[0];
    fileName.textContent = file ? file.name : "Р¤Р°Р№Р» РЅРµ РІС‹Р±СЂР°РЅ";

    if (!file) {
      fileData = "";
      fileMeta = null;
      renderAttachmentCard();
      return;
    }

    fileMeta = {
      name: file.name,
      type: file.type,
      size: file.size,
    };

    const reader = new FileReader();
    reader.onload = () => {
      fileData = reader.result;
      renderAttachmentCard();
    };
    reader.readAsDataURL(file);
  });
}

if (previewButton) {
  previewButton.addEventListener("click", openPreview);
}

document.querySelectorAll("[data-close-modal]").forEach((item) => {
  item.addEventListener("click", closePreview);
});

if (postForm) {
  validatePostForm();

  postForm.addEventListener("submit", async (event) => {
    event.preventDefault();

    if (!validatePostForm() || !window.appService) {
      return;
    }

    const payload = getPostPayload();
    const createdPost = await window.appService.posts.create({
      title: payload.title,
      summary: payload.summary,
      body: payload.body,
      image: fileMeta && fileMeta.type && fileMeta.type.startsWith("image/") ? fileData : "",
      attachment: fileMeta ? {
        name: fileMeta.name,
        type: fileMeta.type,
        data: fileData,
      } : null,
    });

    if (postResult) {
      postResult.classList.remove("hidden");
      postResult.innerHTML = `
        <p class="eyebrow">РџРЈР‘Р›РРљРђР¦РРЇ РЎРћРҐР РђРќР•РќРђ</p>
        <h3>${createdPost.title}</h3>
        <p><strong>Р”Р°С‚Р°:</strong> ${postAppState.formatDate(createdPost.date)}</p>
        ${createdPost.summary ? `<p><strong>РћРїРёСЃР°РЅРёРµ:</strong> ${createdPost.summary}</p>` : ""}
        ${createdPost.attachment ? `<p><strong>Р¤Р°Р№Р»:</strong> ${createdPost.attachment.name}</p>` : "<p><strong>Р¤Р°Р№Р»:</strong> РЅРµ РїСЂРёРєСЂРµРїР»РµРЅ</p>"}
      `;
    }

    postForm.reset();
    setErrorState(titleError, false);
    setErrorState(bodyError, false);
    fileData = "";
    fileMeta = null;
    fileName.textContent = "Р¤Р°Р№Р» РЅРµ РІС‹Р±СЂР°РЅ";
    renderAttachmentCard();
    postStatus.textContent = "РџСѓР±Р»РёРєР°С†РёСЏ СЃРѕС…СЂР°РЅРµРЅР° С‡РµСЂРµР· СЃРµСЂРІРёСЃ.";
    validatePostForm();
    await renderEditorPosts();
  });
}

void renderEditorPosts();

document.addEventListener("app-store-ready", () => void renderEditorPosts());
document.addEventListener("app-store-updated", () => void renderEditorPosts());
```

## requests.js

```js
const requestAppState = window.appState || {
  formatDate(value) {
    const date = new Date(value);
    return Number.isNaN(date.getTime()) ? value : date.toLocaleDateString("ru-RU");
  },
};

const requestList = document.getElementById("request-list");

async function renderRequests() {
  if (!requestList) {
    return;
  }

  const requests = window.appService ? await window.appService.requests.list() : [];

  requestList.innerHTML = requests.length
    ? requests.map((item) => `
      <article class="request-card" data-id="${item.id}">
        <h3>${item.fullname}</h3>
        <p><strong>РўРµР»РµС„РѕРЅ:</strong> ${item.phone}</p>
        <p><strong>E-mail:</strong> ${item.email}</p>
        <p><strong>Р”Р°С‚Р°:</strong> ${requestAppState.formatDate(item.date)}</p>
        <p><strong>РЎРѕРѕР±С‰РµРЅРёРµ:</strong> ${item.message}</p>
        <button class="button ghost delete-request" type="button">РЈР”РђР›РРўР¬</button>
      </article>
    `).join("")
    : '<div class="empty-state">РќРѕРІС‹С… Р·Р°СЏРІРѕРє РїРѕРєР° РЅРµС‚.</div>';

  document.querySelectorAll(".delete-request").forEach((button) => {
    button.addEventListener("click", async () => {
      const card = button.closest(".request-card");
      const id = card?.dataset.id;
      if (!id || !window.confirm("РЈРґР°Р»РёС‚СЊ Р·Р°СЏРІРєСѓ?")) {
        return;
      }
      if (window.appService) {
        await window.appService.requests.remove(id);
      }
      await renderRequests();
    });
  });
}

void renderRequests();

document.addEventListener("app-store-ready", () => void renderRequests());
document.addEventListener("app-store-updated", () => void renderRequests());
```

## lessons.js

```js
const lessonsList = document.getElementById("lessons-list");
const lessonForm = document.getElementById("lesson-form");
const lessonSubmit = document.getElementById("lesson-submit");
const lessonStatus = document.getElementById("lesson-status");
const lessonResult = document.getElementById("lesson-result");
const lessonAdminSection = document.getElementById("lesson-admin-section");
const lessonAccessNote = document.getElementById("lesson-access-note");

const lessonFields = {
  language: document.getElementById("lesson-language"),
  level: document.getElementById("lesson-level"),
  title: document.getElementById("lesson-title"),
  duration: document.getElementById("lesson-duration"),
  goal: document.getElementById("lesson-goal"),
  tasks: document.getElementById("lesson-tasks"),
};

const lessonErrors = {
  language: document.getElementById("lesson-language-error"),
  level: document.getElementById("lesson-level-error"),
  title: document.getElementById("lesson-title-error"),
  duration: document.getElementById("lesson-duration-error"),
  goal: document.getElementById("lesson-goal-error"),
  tasks: document.getElementById("lesson-tasks-error"),
};

let canManageLessons = false;

function setLessonError(element, show) {
  if (!element) {
    return;
  }
  element.classList.toggle("hidden", !show);
}

function syncLessonAccess() {
  if (lessonAdminSection) {
    lessonAdminSection.classList.toggle("hidden", !canManageLessons);
  }
  if (lessonAccessNote) {
    lessonAccessNote.classList.toggle("hidden", canManageLessons);
  }
}

function getLessonPayload() {
  return {
    language: (lessonFields.language?.value || "").trim(),
    level: (lessonFields.level?.value || "").trim(),
    title: (lessonFields.title?.value || "").trim(),
    duration: (lessonFields.duration?.value || "").trim(),
    goal: (lessonFields.goal?.value || "").trim(),
    tasks: (lessonFields.tasks?.value || "")
      .split("\n")
      .map((item) => item.trim())
      .filter(Boolean),
  };
}

function validateLessonForm() {
  const payload = getLessonPayload();
  const checks = {
    language: payload.language.length > 0,
    level: payload.level.length >= 2,
    title: payload.title.length >= 4,
    duration: payload.duration.length >= 3,
    goal: payload.goal.length >= 10,
    tasks: payload.tasks.length >= 1,
  };

  setLessonError(lessonErrors.language, !checks.language);
  setLessonError(lessonErrors.level, !checks.level && payload.level.length > 0);
  setLessonError(lessonErrors.title, !checks.title && payload.title.length > 0);
  setLessonError(lessonErrors.duration, !checks.duration && payload.duration.length > 0);
  setLessonError(lessonErrors.goal, !checks.goal && payload.goal.length > 0);
  setLessonError(lessonErrors.tasks, !checks.tasks && payload.tasks.length > 0);

  const valid = Object.values(checks).every(Boolean) && canManageLessons;
  if (lessonSubmit) {
    lessonSubmit.disabled = !valid;
  }
  if (lessonStatus) {
    if (!canManageLessons) {
      lessonStatus.textContent = "РЎРѕР·РґР°РЅРёРµ СѓСЂРѕРєРѕРІ РґРѕСЃС‚СѓРїРЅРѕ С‚РѕР»СЊРєРѕ Р°РІС‚РѕСЂРёР·РѕРІР°РЅРЅРѕРјСѓ СЃРѕС‚СЂСѓРґРЅРёРєСѓ.";
    } else {
      lessonStatus.textContent = valid ? "РЈСЂРѕРє РіРѕС‚РѕРІ Рє СЃРѕР·РґР°РЅРёСЋ." : "РСЃРїСЂР°РІСЊС‚Рµ РѕС€РёР±РєРё РІ С„РѕСЂРјРµ СѓСЂРѕРєР°.";
    }
  }
  return valid;
}

async function renderLessons() {
  if (!lessonsList) {
    return;
  }

  const lessons = window.appService ? await window.appService.lessons.list() : [];
  lessonsList.innerHTML = lessons.length
    ? lessons.map((lesson) => `
      <article class="lesson-card" data-lesson-id="${lesson.id}">
        <div class="lesson-top">
          <span class="meta-text">${lesson.level}</span>
          <span class="lesson-chip">${lesson.language}</span>
        </div>
        <h3>${lesson.title}</h3>
        <p>${lesson.goal}</p>
        <p><strong>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ:</strong> ${lesson.duration}</p>
        <ul class="service-points">
          ${lesson.tasks.map((task) => `<li>${task}</li>`).join("")}
        </ul>
        <div class="card-actions">
          <a class="button ghost" href="contacts.html">Р—РђРџРРЎРђРўР¬РЎРЇ РќРђ РџР РђРљРўРРљРЈ</a>
          ${canManageLessons ? '<button class="button ghost delete-lesson" type="button">РЈР”РђР›РРўР¬</button>' : ""}
        </div>
      </article>
    `).join("")
    : '<div class="empty-state">РЈСЂРѕРєРѕРІ РїРѕРєР° РЅРµС‚.</div>';

  if (!canManageLessons) {
    return;
  }

  document.querySelectorAll(".delete-lesson").forEach((button) => {
    button.addEventListener("click", async () => {
      const card = button.closest("[data-lesson-id]");
      const lessonId = card?.dataset.lessonId;
      if (!lessonId || !window.confirm("РЈРґР°Р»РёС‚СЊ СѓСЂРѕРє?")) {
        return;
      }
      if (window.appService) {
        await window.appService.lessons.remove(lessonId);
      }
      await renderLessons();
    });
  });
}

async function refreshLessonPermissions() {
  if (window.appService) {
    canManageLessons = await window.appService.auth.status();
  } else {
    canManageLessons = false;
  }
  syncLessonAccess();
  validateLessonForm();
  await renderLessons();
}

Object.values(lessonFields).forEach((field) => {
  if (field) {
    field.addEventListener("input", validateLessonForm);
    field.addEventListener("change", validateLessonForm);
    field.addEventListener("blur", validateLessonForm);
  }
});

if (lessonForm) {
  validateLessonForm();

  lessonForm.addEventListener("submit", async (event) => {
    event.preventDefault();

    if (!validateLessonForm() || !window.appService || !canManageLessons) {
      return;
    }

    const payload = getLessonPayload();
    const lesson = await window.appService.lessons.create(payload);

    if (lessonResult) {
      lessonResult.classList.remove("hidden");
      lessonResult.innerHTML = `
        <p class="eyebrow">РЈР РћРљ РЎРћР—Р”РђРќ</p>
        <h3>${lesson.title}</h3>
        <p><strong>РЇР·С‹Рє:</strong> ${lesson.language}</p>
        <p><strong>РЈСЂРѕРІРµРЅСЊ:</strong> ${lesson.level}</p>
        <p><strong>Р”Р»РёС‚РµР»СЊРЅРѕСЃС‚СЊ:</strong> ${lesson.duration}</p>
      `;
    }

    lessonForm.reset();
    Object.values(lessonErrors).forEach((element) => setLessonError(element, false));
    lessonStatus.textContent = "РЈСЂРѕРє СЃРѕР·РґР°РЅ С‡РµСЂРµР· СЃРµСЂРІРёСЃ.";
    validateLessonForm();
    await renderLessons();
  });
}

void refreshLessonPermissions();

document.addEventListener("app-store-ready", () => void refreshLessonPermissions());
document.addEventListener("app-store-updated", () => void refreshLessonPermissions());
```

